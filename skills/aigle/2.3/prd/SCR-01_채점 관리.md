# [SCR-01] 채점 관리 화면 개발 명세서

## 0. 문서 이력 (Revision History)

- **v3.27** | 2026-08-13 | **배포 후 신규 학생 등록 → 미채점 단계 자동 노출 정책 명문화** —
  - **정책**: 그룹에 배포된 과제가 있어도 학생 관리에서 신규 학생을 그 그룹에 추가할 수 있으며, 추가된 학생은 배포된 과제의 **「미채점」 단계에 자동 재등장**한다 (기존 채점 완료 학생의 완료 상태는 그대로 유지)
  - **개선 배경**: 이전 정책은 「배포된 그룹에 신규 학생 등록 차단」이었으나, 학기 중 전학·편입·명단 누락이 잦아 등록 우회로 운영 부담이 큼. 우려됐던 「완료 과제에 신규 학생이 미채점으로 노출」은 오히려 **자연스러운 재개 흐름**임 (교사가 신규 학생 몫만 이어서 채점)
  - **UI 반영**: SCR-01 미채점 탭 목록에 신규 학생이 자동 포함되며, 기존 학생 상태와 병렬 노출. 배지·강조 표시 없음 — 일반 미채점과 동일 취급
  - **연계 화면**: STU-09 학생 등록 v2.3 (오류 메시지 「배포된 과제가 있는 그룹에는 신규학생을 등록할 수 없습니다.」 폐기)
- **v4.7** | 2026-09-11 | **크래들 일괄 채점 기획을 [SCR-07]로 분리** — 크래들 3대×10구(슬롯 1~10 표기) · 필수 프로그램 [설치 확인]→실행 중→준비 완료 한 흐름 · 2단계 읽는 중 프로그레스 바 · 기재란 OCR 대조 폐기 · 「번호표 재체크」 폐기 → 답안을 보고 **선택 그룹 안에서 직접 매칭**(다른 반 북코드 답안지는 열람만) · 카운트는 배지 색 기준(확인 필요/대상 아님) · 목록 순서 판정 시점 고정 · POP-28 #10 문구 `펜 연결`→`답안 없음 — 답안지 미작성` · 완료 화면 3줄. 상세는 **[SCR-07] 크래들 일괄 채점** 정본 참조. JSX: `CradleGradingModal.jsx` · `RequiredProgramModal.jsx`
- **v4.6** | 2026-09-01 | **스캔 채점 슬롯 상태 정리 — `미지정` 폐기 + `초과`·`부족` 개명 + [← 파일 다시 선택]이 업로드를 초기화** — ⓪ **슬롯 상태 4종 → 3종**(`연결`/`답안지 초과`/`답안지 부족`): 舊 `미지정`(장수 0)은 `부족`과 **차단 여부·빈 장 자리 표현·해소 방법이 모두 같아** 흡수. 배지는 `미지정` → **`답안지 부족 0/2장`**으로 장수를 함께 밝히므로 정보가 오히려 늘고, 색도 중립 회색 → 주의 노랑으로 통일(차단 대상이므로). 파일 쪽 `미분류`와의 **용어 충돌도 해소** ⓪-2 **`초과`·`부족` → `답안지 초과`·`답안지 부족` 개명**: 무엇이 초과·부족인지 이름이 말하지 않아 같은 화면의 `확인 필요`(장 단위)·`미분류`(파일 단위)와 층위가 섞여 보였다. 차단 사유 문구도 `답안지 초과 N건 · 답안지 부족 N건 · 확인 필요 N장`으로 갱신. 좌측 학생×문항 매트릭스는 라벨이 길어져 문항 칸 폭 66→78px, 패널 폭 352→392px. 이어서 이어서 ① 연결 결과 확인에서 [← 파일 다시 선택]을 누르면 **업로드 파일·OCR 판별 결과·선택/메뉴·미리보기를 모두 비우고** 파일 업로드 단계로 돌아간다 ② **舊 동작의 문제** — 화면만 되돌아가고 파일이 남아 있어, 다시 고른 파일이 기존 파일에 **더해졌다**(중복 업로드) ③ 파일 목록의 **[전체 삭제]와 같은 동작**으로 통일 ④ 초기화 직후 **직전과 동일한 파일을 다시 선택해도** 업로드된다 ⑤ 확인 다이얼로그는 두지 않는다 — 버튼 이름이 이미 다시 고른다는 뜻이다. 상세는 **[SCR-05] 스캔 일괄 채점 §3 되돌아가기** 정본 참조. JSX: `ScanGradingModal.jsx`(`resetUploads()` 신설, 파일 선택 input 값 초기화)
- **v4.5** | 2026-08-31 | **일괄 채점 완료 단계에 채점 요약(문항 수 · 학생 수) 반영** — ① 舊 초록 배너 `채점 완료: 모든 펜의 채점이 끝났습니다.` 한 줄을 **SCR-05 스캔 채점 완료 단계와 같은 요약 블록**으로 확장 ② **표기 항목** — 제목 `일괄 채점이 완료되었습니다.` + `채점 문항 N건 · 학생 M명` + 마무리 안내 ③ **집계 기준** — `selectedIds`는 완료 시점에 비워지므로 스냅샷 `lastBulkGradedIds`로 계산. 학생 수 = 그중 `채점 확인`으로 전환된 수, 문항 수 = 학생 수 × 문항 수 ④ **실패 고지 신설** — 채점되지 않은 학생이 있으면 `⚠ N명은 채점되지 않아 미채점 탭에 그대로 남습니다. 펜 데이터를 확인한 뒤 다시 실행해 주세요.` ⑤ **문구는 과거형** — 펜 경로는 상태 전환이 **채점 완료 시점에 이미 끝나고** 버튼은 [닫기]이므로, 스캔의 「[확인]을 누르면 … 이동합니다」가 아니라 **`전 문항이 채점된 N명이 「채점 확인」 단계로 이동했습니다.`**로 쓴다 ⑥ **채택 사유** — 「몇 명이 채점됐고 몇 문항이 처리됐는지」는 경로(펜/스캔)와 무관하게 교사가 확인해야 하는 값인데, 舊 배너는 「끝났다」만 말하고 규모를 말하지 않았다. JSX: `GradingManagement.jsx`(`bulkResultStudents`·`bulkGradedCount`·`bulkFailedCount`·`bulkGradedQuestionCount` 파생 신설, `final_bulk` 완료 배너 교체)
- **v4.4** | 2026-08-31 | **스캔 채점 — 기존 답안을 답안지 카드로 승격, 스캔과 동일 취급** — ① **기존 답안이 슬롯에 카드로 올라옴**: `답안 있음` 학생의 기존 답안을 연결 결과 확인 화면에 스캔 답안지와 **같은 카드 형식**으로 표시. 두 종류 데이터가 한 화면에 나란히 섬 ② **`기존 답안 교체` 예고 칩 폐기** — 카드가 이미 상태를 말하므로 예고 불필요. 교체가 끝난 뒤에만 `🔄 교체됨` 결과 표기 ③ **교체는 공통 메뉴로** — 카드의 [⋯ → 파일 업로드](로컬 스캔본) 또는 [⋯ → 미분류 파일]. 스캔 답안지와 완전히 같은 조작 ④ **카드 내용** — 좌측 `OCR 판독` → **`제출 정보`**(제출 경로·제출일시), 우측 `연결 결과`는 동일, 신뢰도 배지 자리에 **`📄 기존 답안`** 출처 배지 ⑤ **기존 답안은 「장」이 아니라 「제출 1건」** — 기준 장수와 비교하지 않으며 붙은 슬롯은 항상 `연결`, 빈 장 자리도 만들지 않음 ⑥ **채점 대상** — 교체하지 않고 둔 기존 답안도 채점(교체하면 스캔본으로, 두면 기존 답안으로) ⑦ **되돌리기 신설** — 교체한 스캔본을 [⋯ → 연결 해제]하면 기존 답안 카드가 복원됨. 이 복원이 없으면 잘못 교체한 순간 `미지정`이 되어 v4.3 차단에 걸리고 되돌릴 방법이 없음 ⑧ **기존 답안 카드에는 [연결 해제] 없음** — 파일이 아니라 미분류로 내려갈 자리가 없음 ⑨ **실행 직전 확인 집계 기준 변경** — 舊 「`답안 있음` 학생 + 장수 ≥ 1」 → **「실제로 교체된 슬롯」** ⑩ **채택 사유** — 舊 모델은 기존 답안이 보이지 않는 플래그여서 교사가 무엇을 덮어쓰는지 모른 채 경고만 받았음. 카드로 올리면 그 자리에서 확인하고 판단 가능. 상세는 **[SCR-05] 스캔 일괄 채점 §4.5·§5.2·§6** 정본 참조. JSX: `ScanGradingModal.jsx`(`buildExistingRow()` 신설, `slotHasExisting`·`slotReplacedExisting` 파생, `slotStatus` 기존 답안 분기, `fileCard` 기존 답안 렌더)
- **v4.3** | 2026-08-31 | **스캔 채점 [채점 시작] 사전 차단 — 초과·부족·미지정·확인 필요가 남으면 비활성** — ① **차단 전환**: v4.2는 이 상황들을 통과시키고 완료 요약에서 고지만 했으나, 「데이터가 덜 갖춰진 채로 채점이 확정된다」는 문제로 **사전 차단**으로 되돌림 ② **차단 조건**: 슬롯이 하나라도 `초과`·`부족`·`미지정`이거나 연결된 답안지에 `확인 필요`(신뢰도 medium)가 남아 있으면 비활성. **모든 슬롯이 `연결` 상태여야 시작 가능** ③ **미분류 파일은 무시** — 어느 슬롯에도 붙지 않아 채점에 관여하지 않으므로 차단 사유에서 제외 ④ **사유 노출** — 푸터·툴팁에 `초과 N건 · 부족 N건 · 미지정 N건 · 확인 필요 N장을(를) 먼저 정리해 주세요.` 표기 ⑤ **「확인 필요」도 차단 대상** — v4.1 §7.1의 「확인 필요는 채점을 막지 않는다」 폐기. AI 추정 문항이 검증되지 않은 채 채점되면 잘못된 문항에 답안이 붙음 ⑥ **부분 채점 소멸** — 모든 슬롯이 정상이어야 시작되므로 `부족` 채점·일부 문항만 채점된 학생이 발생하지 않음. §8.2 부분 채점/부족 장수 고지는 도달하지 않는 경로가 되나 정책 완화 대비로 코드·문구는 유지 ⑦ **잔여 위험 명시** — `부족`은 학생이 짧게 쓴 정상 케이스와 구별되지 않아, 채울 답안지가 없으면 교사가 채점을 시작할 수단이 없음. 우회로는 [TSK-02] 기준 장수 조정뿐. 운영 사례 발생 시 예외 수단 재검토. 상세는 **[SCR-05] 스캔 일괄 채점 §5.6·§7** 정본 참조. JSX: `ScanGradingModal.jsx`(`abnormalSlots`·`pendingCheckFiles`·`startBlocked`·`startBlockReason` 신설, 舊 `incompleteSlots` 제거)
- **v4.2** | 2026-08-31 | **스캔 채점 — 덮어쓰기 3단 게이트 → 단일 게이트 + 수동 연결 지정 폐기 + 누락 안내 폐기 + 모달 최소화(FAB)** — ① **舊 1단(대상 학생 확인) 폐기**: 파일 업로드 단계의 포함/제외 셀렉트 삭제, `답안 있음` 학생도 **선택된 전원이 OCR 판별 대상**. 그 자리엔 고지 배너만 ② **舊 2단(문항별 [교체 승인] 토글) 폐기**: 연결된 교체 슬롯은 즉시 채점 대상, 표기도 `기존 답안 교체` 정적 칩으로 단일화 ③ **실행 직전 확인만 유지** — 교체 1건 이상이면 다이얼로그 1회, **유일한 방어선** ④ **연결 지정 → 데이터 표시**: 답안지 카드 우측 「✏ 연결 지정」(이름 입력 + 문항·장 셀렉트)을 **「🔗 연결 결과」 읽기 전용**으로 전환. 보정은 답안지 작업 공통 메뉴로 일원화 ⑤ **미분류는 차단 사유가 아님**: [채점 시작] 활성 조건은 「연결된 슬롯 ≥ 1건」 하나뿐 ⑥ **부분 제출 3지선다 폐기**: 「답안이 누락된 학생이 있습니다」 다이얼로그 삭제, 누락은 완료 요약에서만 고지 ⑦ **모달 최소화 + FAB 복귀 신설**: `AI 일괄 채점`·`완료`에서 창을 닫으면 모달이 종료되지 않고 최소화(mount 유지)되며 채점은 계속 진행. FAB 클릭 → 완료 창 복귀 → **[확인] → 「채점 확인」 탭 자동 이동** (舊 흐름은 확인 이후 FAB → 첫 학생 SCR-03 상세 오픈) ⑧ **채택 사유**: 舊 1단은 교사가 **판별 결과를 보기도 전에** 결정을 요구했고 실제로는 대부분 전원 포함이었음. 수동 지정 UI는 「전량 검수」 신호를 주고 「미분류를 지정해야 채점된다」는 오해를 낳았음. 매핑 완료 시점엔 무엇이 채점·제외되는지 화면에 이미 드러나 있어 같은 정보를 다이얼로그로 다시 묻지 않음 ⑨ **잔여 위험 명시**: 덮어쓰기 방어선이 1개로 축소되고 미분류·누락은 교사 개입 없이 제외됨. 완료 요약 고지 + 「미채점 탭 잔류」 + 기존 답안 이력 보관이 최종 안전망. 상세는 **[SCR-05] 스캔 일괄 채점 §5.2·§5.6·§6·§7·§8.3** 정본 참조. JSX: `ScanGradingModal.jsx`(`includedAnswerIds`·`overwriteConfirmedSlots`·`confirmPartial`·`partialPolicy`·`canCloseNow` 제거, `onMinimize`·`onGradingFinished` prop 신설) + `GradingManagement.jsx`(`scanMinimized` state, `handleScanMinimize`, FAB 분기, `setActiveTab('채점 확인')`)
- **v4.1** | 2026-08-30 | **스캔 일괄 채점 리뷰 화면 단일화 + 답안지 코드 장 단위 전환** — ① 뷰 3종(`상태별`·`학생 × 문항`·`파일 목록`)을 **「학생 × 문항」 하나로 고정**하고 좌(학생 목록)/우(문항 3열 상세) **2단 마스터-디테일**로 재구성. 각 패널 독립 스크롤이라 학생 30명이어도 상세를 보러 화면을 내리지 않는다 ② **답안지 코드를 장 단위로 전환** — 舊 `taskCode`(과제당 1개) → `sheetCode`(장당 1개). 「타 과제 답안지」 판정도 코드 집합 포함 여부로 변경 ③ **장 단위 지정** — 문항 셀렉트가 `문항 1-1 / 문항 1-2`까지 지원(`sheetNo`) ④ **빈 장 카드 신설** — `short`·`missing`을 비어 있는 장 자리로 렌더, 그 자리에서 바로 채운다 ⑤ **답안지 작업 공통 메뉴** — 파일 업로드(버튼)/미분류 파일(목록)/연결 해제(버튼) 3구역 팝오버 ⑥ **슬롯 단위 직접 업로드** — `upload` Step 복귀 없이 그 자리에서 파일 추가·교체 ⑦ `결손` → **`미지정`** 개명, 표시 원칙을 「정상은 조용히, 이상만 드러내기」로 통일 ⑧ **미분류·채점 진행 규칙 명문화** — 연결 해제/미매칭 파일이 채점에 어떻게 반영되는지 상태별 정의 ⑨ 모달 `1280px` → **`92vw × 90vh`**. 상세는 **[SCR-05] 스캔 일괄 채점 §5·§7.1** 참조. JSX: `ScanGradingModal.jsx`
- **v4.0** | 2026-08-12 | **스캔 일괄 채점 다문항 대응 — 연결 단위를 「학생」 → 「슬롯(학생 × 문항)」으로 전환** — 한 과제에 문항이 여럿이고 문항마다 답안지 장수가 다른 실제 운영을 舊 모델(`1파일 = 1학생`)이 표현하지 못했다. ① **판별 방식 A안 확정** — 파일명 규칙·QR 없이 답안지 기재 내용(과제코드·학년/반/번호·이름·문항 번호)만으로 OCR 판별. QR 미채택 근거는 **실패 모드의 비대칭**(OCR 실패 = 미분류로 빠져 교사 확인 / QR 오류 = 조용히 잘못 채점) ② **신뢰도 3단계 트리아지** 🟢자동 확정/🟡확인 필요/🔴미분류 — 교사가 전량이 아니라 애매한 것만 확인 ③ **리뷰 화면 재구성** 파일 목록 단일 뷰 → **학생 × 문항 매트릭스**(기본) + 미분류 트레이 + 파일 목록(보조). 결손은 행이 없어 생기는 문제라 파일 목록으로는 보이지 않았음 ④ **슬롯 상태 4분류** `ok`/`over`/`short`/`missing` (기준 = TSK-02 「답안지 출력 장수 설정」). 舊 `duplicate` 폐기 — 한 슬롯 복수 파일이 정상이 되어 「기준 장수 초과」로 흡수 ⑤ **덮어쓰기 확인 단위** 파일 → **슬롯** ⑥ **부분 제출 3지선다 신설** 있는 답안으로 계속 채점(권장)/누락 학생 제외/취소하고 파일 추가 ⑦ **학생 상태 전환 조건 명문화** — **전 문항이 채점된 학생만** `채점 확인`으로 이동, 일부만 채점된 학생은 `미채점` 유지(새 상태값 미신설). 상세는 **[SCR-05] 스캔 일괄 채점** 정본 문서 참조. 연계: [TSK-02 v3.3] 답안지 출력 장수, [TSK-05 v3.5] 답안지 서식. JSX: `ScanGradingModal.jsx` 전면 개편 + `GradingManagement.jsx` prop 배선·`questions[].sheets`
- **v3.26** | 2026-08-12 | **스캔 일괄 채점 모달 크기 확대** — 본 모달 `900px` → **`1280px`**(`maxWidth 96vw` → `95vw`), `minHeight: 72vh` 신설 + `maxHeight 92vh` 유지. 미리보기 팝업 `720px` → **`960px`**, mock 답안지 렌더 폭 `500px` → `620px`, 이미지 미리보기 `maxHeight 70vh` → `74vh`. **근거**: 리뷰 Step 4열 테이블이 900px에서 과밀했고 v3.24에서 상태 열에 덮어쓰기 토글이 추가되며 더 좁아짐. `minHeight`는 단계 전환 시 모달 크기가 출렁이는 문제 완화용. 1600×1000 / 1366×768 양쪽에서 뷰포트 이탈·가로 스크롤 없음 확인. 상세는 **[SCR-05] 스캔 일괄 채점 §3 모달 크기** 참조. JSX: `ScanGradingModal.jsx`
- **v3.25** | 2026-08-12 | **스캔 채점 1단 게이트 —「대상 학생 확인」 행의 포함/제외 컨트롤을 체크박스 → 셀렉트박스로 변경**
  - **舊(v3.24)**: 행 좌측 체크박스 + 행 우측에 현재 상태 텍스트(`제외됨 (답안 유지)` / `🔄 덮어쓰기 대상에 포함`)를 별도로 표기. 컨트롤과 상태 표시가 행 양끝으로 분리되어 있고, 체크박스는 「선택하지 않았을 때 무엇이 되는지」를 스스로 설명하지 못함
  - **신규**: 행 우측 셀렉트박스 1개로 통합. 두 선택지를 **모두 라벨로 노출**하여 교사가 무엇을 고르는 중인지 즉시 인지
    - `제외 (답안 유지)` — **기본값**. 학생이 제출한 답안을 그대로 유지, 스캔 파일이 매칭되어도 채점 대상에서 제외
    - `🔄 포함 (스캔본으로 교체)` — 스캔본으로 기존 답안을 교체하는 대상에 포함
  - **시각 처리**: `포함` 선택 시 셀렉트 보더·텍스트를 주황 톤(`#FB923C` / `#C2410C`)으로, `제외`는 회색 톤(`#CBD5E1` / `#64748B`)으로 전환. 행 보더 색상 전환은 v3.24와 동일하게 유지
  - **접근성**: 셀렉트에 `aria-label="{학생명} 스캔 채점 대상 여부"` 부여. 舊 구조는 행 전체가 `<label>`이라 학생명·칩까지 클릭 대상이 되어 오조작 여지가 있었으나, 신규는 `<div>` + 셀렉트로 클릭 영역을 컨트롤에 한정
  - **정책 불변**: 기본 제외 정책·매칭 결과 즉시 재계산(`buildStatuses`)·2단/3단 게이트는 v3.24 그대로. 본 변경은 **1단 게이트의 입력 방식만** 교체
  - **JSX**: `ScanGradingModal.jsx` — `toggleAnswerStudent(id)` → **`setAnswerStudentIncluded(id, include)`**(명시적 값 전달)로 교체, 행 렌더를 `<label>`+체크박스 → `<div>`+`<select>`로 변경
- **v3.24** | 2026-08-12 | **상태 배지 `OCR 제출` → `답안 있음` 명칭 변경 + 「스캔 채점 — 답안 기제출 학생 처리 정책」 신설**
  - **① 명칭 변경 근거**: 舊 `OCR 제출`은 두 가지 의미 충돌이 있었음. (1) 스캔 채점의 OCR은 **학생 번호 인식**을 뜻하는데 상태명의 OCR은 **답안 제출 경로**를 뜻해 같은 단어가 다른 개념을 가리킴 (2) 학생이 **키보드로 입력**해 제출한 경우도 OCR이 아닌데 `OCR 제출`로 표시됨. → 제출 수단과 무관하게 「채점 전이지만 답안이 존재한다」는 사실만 나타내는 **`답안 있음`**(파랑)으로 통일
  - **② 상태 의미 명문화**: `미채점`(빨강) = 과제 배포 후 **답안 없음** / `답안 있음`(파랑) = **채점 전이지만 학생 답안이 이미 존재**(파일 업로드·키보드 입력 등). 양쪽 모두 `status === '미채점'`의 하위 구분이며 미채점 탭에 함께 노출된다 (탭 구조·필터 변경 없음)
  - **③ 신설 정책 — 스캔 채점 시 `답안 있음` 학생 처리**: 스캔 채점은 교사가 종이 답안지를 시스템에 **주입**하는 행위이므로, 이미 답안이 있는 학생에 매칭되면 **기존 학생 답안이 덮어써진다**. 완전 차단(hard block)이 아닌 **「선택 허용 + 덮어쓰기 명시 확인」(soft block) 3단 게이트**로 처리 (상세는 [SCR-05] 스캔 일괄 채점 §6 참조)
  - **④ 매칭 상태 4분류로 확장**: `ok` / `unmatched` / `duplicate` + **`overwrite`(🔄 덮어쓰기) 신규**. 상태 우선순위는 **중복 > 덮어쓰기 > 매칭·미매칭** — 즉 **중복 해소가 항상 먼저**이며, 정본 파일이 확정된 뒤에야 덮어쓰기 확인 단계로 넘어간다
  - **⑤ 매칭 상태 재계산 방식 변경**: 舊 「편집된 행만 판정」 → **전체 행 일괄 재계산(recompute)**. 한 학생에 두 파일이 걸리면 **양쪽 행 모두** 중복으로 표시되고, 중복 해소 즉시 남은 행이 자동으로 `overwrite` 또는 `ok`로 승격된다
  - **⑥ JSX**: `GradingManagement.jsx` 카드/리스트 배지 라벨 변경(2곳). `ScanGradingModal.jsx` — `answerStudentIds`·`includedAnswerIds`·`overwriteConfirmedIds`·`confirmOverwrite` state 신규, `recomputeStatuses()` 도입, 업로드 Step 대상 학생 게이트 카드 추가, 리뷰 Step 카운트 타일 3→4개 + 행별 [덮어쓰기] 토글, 채점 시작 직전 확인 다이얼로그 추가
- **v3.23** | 2026-06-09 | **개인정보 보호 안내 위치를 페이지 타이틀 옆 인라인으로 이동** — v3.22에서 task-info-banner와 control-bar 사이에 배치했으나, 화면이 한 화면에 다 보이는 컴팩트한 형태가 더 적합하다는 의견. 페이지 헤더(「채점 관리」 page-title)와 같은 줄에 인라인 칩 형태로 배치. ① **위치 변경**: `task-info-banner` 위 → `header.content-header` 안 page-title **오른쪽 옆**. ② **JSX 구조**: 기존 단일 `.page-title` div 대신 flex container로 감싸 `[채점 관리]` + `[🔒 안내 칩]`을 가로 정렬(`gap: 14px`, `flexWrap: wrap`). 좁은 폭에서는 자동 줄바꿈. ③ **디자인 컴팩트화**: 8px radius, padding 0.4rem 0.75rem, 폰트 0.78rem (이전 0.83rem). 그린 토큰은 유지(`#F0FDF4` 배경 + `#86EFAC` 보더 + 🔒 + `#166534` 텍스트). 메시지 본문 동일. ④ **장점**: 페이지 첫 시선 = 「채점 관리」와 동시에 안심 메시지 인지. 컨텐츠 영역(task-info-banner·control-bar·학생 목록)이 한 줄 더 위로 올라와 본문이 더 많이 보임. ⑤ **§3 「페이지 헤더 영역」**: 인라인 칩 배치 사양 갱신.
- **v3.22** | 2026-06-09 | **개인정보 보호 안내 배치를 채점 목록 화면 1곳으로 통합 — v3.21의 일괄 모달 + SCR-02 footer 분기 폐기** — v3.21에서 일괄 채점 모달과 SCR-02 미채점 상세 모달 두 곳에 동일 메시지를 분기 배치했으나, 사용자 의견: 「채점 목록에 1회만 노출」 정책으로 변경. 같은 메시지를 2곳에서 보면 피로감만 늘고 의사 결정 지점도 분산. ① **신규 위치**: SCR-01 채점 관리 메인 화면 — `task-info-banner`(과제 메타 영역) 아래, `control-bar` **바로 위**. 채점 관리 진입 시 1회 노출되며 탭(미채점·채점 확인·결과 발송) 무관하게 동일 표시. ② **롤백**: GradingManagement.jsx 일괄 채점 모달 `instruction` 단계의 안내 박스 제거(v3.21 추가분), UngradedDetailModal.jsx footer 위 안내 박스 제거(SCR-02 v2.7 추가분). ③ **디자인**: 동일 그린 토큰 유지(`#F0FDF4` 배경 + `#86EFAC` 보더 + 🔒 + `#166534` 텍스트). 메시지 본문 동일: 「🔒 **AI 채점에는 학생 답안 내용만 전송**되며, 이름·학번 등 **개인 식별 정보는 포함되지 않습니다.**」 ④ **노출 정책**: 채점 관리 화면이 열려 있는 동안 영구 노출(모달 진입 후 사라지지 않음). 화면을 벗어나면 자연 사라짐. ⑤ **SCR-02 paired**: v2.8에서 footer 안내 박스 폐기 정책 동기화. ⑥ **§3 「채점 관리 화면 상단 영역」**: 안내 박스 위치·디자인 명시.
- **v3.21** | 2026-06-09 | **개인정보 보호 안내 박스 추가 — 일괄 채점 워크플로우 모달 (`bulkStep === 'instruction'` 단계)** — AI 채점 실행 직전 시점에 학교 관리자·교사가 가장 자주 묻는 「우리 학생들의 개인정보가 AI한테 전달되는가?」 우려에 대한 사전 안심 메시지. ① **위치**: 「💡 일괄 채점 안내」 박스 바로 아래, [취소][채점 시작하기] 버튼 위. ② **디자인**: 그린 배경(`#F0FDF4`) + 그린 보더(`#86EFAC`) + 12px radius + flex gap. 🔒 아이콘 + 본문 2줄. 본문 `#166534` 텍스트, 「학생 답안 내용만 전송」과 「개인 식별 정보는 포함되지 않습니다」를 굵게 강조. ③ **메시지**: 「🔒 **AI 채점에는 학생 답안 내용만 전송**되며, 이름·학번 등 **개인 식별 정보는 포함되지 않습니다.**」 ④ **노출 시점**: 일괄 채점 모달의 `instruction` 단계(채점 직전)에서만. 모달 닫히면 자동 사라짐 — 영구 배너 아님. ⑤ **SCR-02와 일관**: 개별 [AI 채점 시작] 클릭 직전에도 동일 메시지가 SCR-02 미채점 상세 모달의 footer 위에 노출됨(SCR-02 v1.10 paired). 일괄/개별 동일 디자인 토큰. ⑥ **§3 「일괄 채점 모달 안내 영역」**: 안내 박스 사양 명시.
- **v3.20** | 2026-06-09 | **모든 탭의 일괄 액션 버튼을 view-controls(상단)로 통합 + 하단 floating 영역 폐기** — v3.17에서 미채점 탭의 [일괄 채점]만 상단으로 승격하고 다른 탭(채점 확인·결과 발송)의 일괄 액션은 하단 floating에 남겨 두었으나, 탭별로 액션 위치가 달라 사용자가 위/아래를 번갈아 보는 동선 문제 + 시선 흐름 일관성 결여. ① **상단 통합 대상 5개 액션**: 채점 확인 탭 [✓ 검토 완료 처리 (N명)] · [✎ 일괄 과정평가 (N명)](수학 한정), 결과 발송 탭 [📤 결과발송 (N명)] · [↩ AI 재검토 (1명)] · [🚫 결과발송 취소 (N명)]. 모두 view-controls 영역 안에 [그룹 ▾][☑ 전체 선택] 뒤, 뷰 토글 [▦][☰] 앞 위치로 통합. 노출 조건(`bulkReviewCompleteIds`·`bulkPreSendIds`·`bulkSentIds` 등)·색상 토큰(`#E8590C`·`#8B5CF6`·`#2A75F3`·`#EF4444`·white+red border)·disabled 정책(isAnyBgActive 차단)은 그대로 유지. ② **view-controls flex-wrap 적용**: 좁은 폭에서 자연 줄바꿈 (`flexWrap: wrap, rowGap: 8`). 뷰 토글은 여전히 `marginLeft: auto`로 가능한 가장 우측. ③ **하단 floating 영역 폐기**: 舊 `bulk-button-container` div 통째로 삭제. v3.17에서 미채점 탭만 옮겨두고 남겨두었던 잔여 컨테이너 완전 제거. ④ **§3 「탭 & 컨트롤 바」 갱신**: 일괄 액션 버튼 5개가 view-controls 안에 인라인 노출됨을 명시. 탭별 노출 매트릭스 추가.
- **v3.19** | 2026-06-09 | **그룹 선택 기본값 자동 설정 — 미채점 첫 번째 학생의 그룹** — 기존에는 화면 진입 시 그룹 셀렉트가 placeholder 「그룹 선택」 상태로 비어 있어 사용자가 매번 명시적으로 그룹을 골라야 일괄 채점을 시작할 수 있었음. 자연 흐름(그룹 → 전체 선택 → 일괄 채점)의 첫 단계를 자동 완료. ① **로직**: `useEffect` mount 1회에서 `students.find(s => s.status === '미채점')`의 `grade` 필드(`예: '1학년 1반 1번'`)에서 정규식 `/\s*\d+번\s*$/`로 「N번」 꼬리 제거 → 그룹명(`'1학년 1반'`)을 `setSelectedGroup`. ② **조건**: 사용자가 이미 그룹을 변경했으면(`selectedGroup`이 비어있지 않으면) skip. 미채점 학생이 0명이면 skip(빈 상태 유지). ③ **mock 정합**: 현재 mock에서는 김순정(1학년 1반 1번)이 첫 미채점 학생 → `'1학년 1반'`이 자동 선택. 그룹 옵션 목록에 동일 라벨이 존재하므로 정합. ④ **§3 그룹 셀렉트박스 항목**: 기본값 정책 명시.
- **v3.18** | 2026-06-09 | **control-bar 2단 레이아웃 + 「전체」 탭 폐기** — v3.17에서 탭(좌) + view-controls(우) 같은 줄에 배치했으나, 화면 폭이 좁아지면 view-controls가 줄바꿈되어 정렬이 깨지고 시선 흐름도 「탭 → 옆 view-controls」가 아닌 「탭 → 아래 학생 카드 → 다시 위 view-controls」로 꼬이는 문제. ① **2단 레이아웃**: control-bar에 `flexDirection: column` 적용. **1단**: 필터 탭 / **2단**: `[그룹 선택 ▾]` → `[☑ 전체 선택]` → `[일괄 채점 (N명)]`(미채점 탭 한정) → `[▦] [☰]`. view-controls는 좌측 정렬(`justifyContent: flex-start`). ② **「전체」 탭 폐기**: 4개 탭(전체·미채점·채점 확인·결과 발송) → **3개 탭(미채점·채점 확인·결과 발송)**. 이유: 「전체」는 상태 혼합으로 일괄 액션을 적용할 수 없는 탭이라 실무 가치 낮음. 교사 워크플로우는 「미채점 처리 → 검토 완료 → 결과 발송」 3단계 핵심에 집중. ③ **기본 탭**: 화면 진입 기본값 `전체` → `미채점`으로 변경(교사가 가장 먼저 처리할 탭). ④ **분기 코드 정리**: `filteredStudents`·`getTabLabel`·`activeTab` 초기값에서 `'전체'` 분기 제거. 다른 `setActiveTab('미채점')` 자동 이동 로직(채점 완료 후·완료 토스트 이후)은 영향 없음. ⑤ **§3 「탭 & 컨트롤 바」**: 탭 목록 갱신 + control-bar 2단 레이아웃 명시.
- **v3.17** | 2026-06-09 | **「미채점 탭 control-bar 자연 흐름 정렬」 — [그룹 선택 → 전체 선택 → 일괄 채점] 한 줄 배치** — 기존 view-controls는 ① [전체 선택] (좌) ② [▦ 카드] / [☰ 목록] (중) ③ [그룹 선택 ▾] (우) 순서로 배치되어, 사용자 자연 흐름(그룹 정하기 → 학생 전체 선택 → 일괄 채점 실행)과 좌우 동선이 어긋나는 문제 있었음. 게다가 일괄 채점 버튼은 하단 floating 영역에 따로 떨어져 있어 control-bar에서 바로 실행할 수 없었음. ① **view-controls 순서 재정렬**: `[그룹 선택 ▾]` → `[☑ 전체 선택]` → `[일괄 채점 (N명)]`(미채점 탭 + 선택자 1명 이상 시 인라인 노출) → `[▦ 카드]` `[☰ 목록]`(뷰 토글은 우측 끝). ② **하단 floating의 미채점 탭 일괄 채점 버튼 제거** — control-bar로 승격된 만큼 중복 노출 방지. 다른 탭(채점 확인·결과 발송)의 일괄 액션 버튼([✓ 검토 완료]·[✎ 일괄 과정평가]·[📤 결과발송]·[↩ AI 재검토]·[🚫 결과발송 취소])은 이번 변경 범위 외로 기존 하단 floating 위치 유지. ③ **버튼 상태**: 기존 disable 정책(isAnyBgActive 차단 + 안내 툴팁) 유지. ④ **§3 control-bar 영역 갱신**: view-controls 항목 순서·미채점 탭 일괄 채점 인라인 노출 조건 명시.
- **v3.16** | 2026-05-26 | 일괄 [↺ 미채점 처리] 버튼 제거 — 채점 확인 탭의 일괄 액션 목록에서 [↺ 미채점 처리]를 제거. 영향 범위가 큰 액션(채점 결과·교사 피드백·과정평가 결과 폐기)이므로 **학생 단위 신중 처리 정책**으로 변경. 본 액션은 **SCR-03 상세 모달 푸터**에서만 가능. JSX(GradingManagement.jsx)의 `bulkRevertToUngradedIds`·`handleBulkRevertToUngraded`·UI 버튼도 동반 제거
- **v3.15** | 2026-05-21 | 「채점 확인 단계 일괄 버튼」 섹션 신설 — [✓ 검토 완료 처리] 명세 정식화 + **[↺ 미채점 처리 (N명)]** 일괄 액션 신규 (체크된 `채점 확인` 학생을 `미채점`으로 환원, 채점 산출물 폐기, 미채점 탭 자동 이동). AI 채점 2회 카운터는 환원 후에도 보존. SCR-03 푸터의 동일 액션과 paired
- **v3.17** | 2026-07-15 | **스캔 채점 워크플로우 — 스캔 파일 미리보기 기능 추가**:
  - **위치**: `ScanGradingModal`의 파일 리스트(업로드 Step) + 매칭 결과 테이블(리뷰 Step)
  - **트리거**: 각 파일 행 우측 [👁 미리보기] 버튼 클릭
  - **미리보기 모달 spec** (별도 z-index 상위 팝업 `9750`):
    - 헤더: 파일 아이콘·파일명·용량·(리뷰 단계) OCR 인식 번호·상태 배지 함께 노출
    - 본문 (파일 종류별 분기):
      - **이미지 파일 (`kind === 'image'`)**: `URL.createObjectURL`로 실제 이미지 렌더 + **OCR 인식 영역 하이라이트 오버레이** (답안지 우상단 32% × 10% 노란 점선 박스, 「학생 번호 OCR 영역」 라벨)
      - **Mock 파일 (`kind === 'mock'`, 데모 파일)**: 답안지 mock 레이아웃(QiGLE 로고 + 정보 테이블 + 라인 영역) + 우상단에 OCR 영역 박스에 실 인식 번호 표시
      - **PDF 파일 (`kind === 'pdf'`)**: 「PDF 미리보기 준비 중」 placeholder + 파일 매니저에서 직접 확인 안내 (pdfjs 렌더링은 후속 단계)
      - **미지원 형식**: 안내 문구만 표시
    - 푸터 (리뷰 단계 한정): 「이 파일을 어느 학생에게 매칭할까요?」 select + [적용 후 닫기] 버튼 → 팝업에서 바로 매칭 수정 가능
  - **UX 근거**: 미매칭 파일은 OCR로 학생 번호 인식 실패 케이스가 다수. 파일 내용을 즉시 확인해야 정확한 수동 매핑이 가능함. 리스트에서 팝업까지 원-스텝 확인 흐름
  - **메모리 관리**: 이미지 파일 업로드 시 `URL.createObjectURL` 생성, 파일 삭제·모달 언마운트 시 `URL.revokeObjectURL`로 정리
  - **아이콘 규칙**: 이미지 🖼 / PDF 📕 / 데모·기타 📄
  - **JSX**: `src/ScanGradingModal.jsx` `previewFileId` state 신규, useEffect로 unmount 시 URL 정리, 미리보기 모달 렌더 분기 추가
- **v3.16** | 2026-07-15 | **스캔 일괄 채점 신규 추가 — [📷 스캔 채점] 버튼 + `ScanGradingModal` 워크플로우**:
  - **위치**: 미채점 탭 컨트롤 바, [일괄 채점 (N명)] 버튼 우측에 [📷 스캔 채점 (N명)] 신규 (하늘색 톤 `#0EA5E9`)
  - **활성 조건**: 그룹 선택 + 학생 1명 이상 선택 (기존 [일괄 채점]과 동일)
  - **비활성 조건**: 백그라운드 채점 진행 중(`isAnyBgActive`) — 기존 일괄 버튼과 동일 정책
  - **핵심 아이디어**: **스캔 파일을 학생 순서대로 정렬할 필요 없음.** 답안지 상단의 학생 번호 영역(OCR)을 읽어 그룹 학생과 자동 매핑
  - **워크플로우 5-Step** (`ScanGradingModal.jsx`):
    1. `upload` — 다중 스캔 파일 업로드 (PDF · PNG · JPG, 파일당 20MB 이하)
    2. `matching` — OCR 실행 + 학생 번호 인식 + 그룹 학생 매핑 (mock 1.5초, 상용 시 실 OCR API 연동)
    3. `review` — 매칭 결과 검토 (성공 / 미매칭 / 중복 3 섹션 카운트 + 상세 테이블)
    4. `grading` — AI 일괄 채점 진행 (매칭 성공 학생만 대상, 5초 mock)
    5. `completed` — 완료 요약 (성공 X명 / 스킵·실패 Y건) → [✓ 확인] 클릭 시 상위 콜백
  - **매칭 상태 3분류**:
    - **✅ 매칭 (`ok`)**: OCR로 인식된 학생 번호가 선택된 그룹 학생 목록에 존재
    - **⚠ 미매칭 (`unmatched`)**: OCR 인식 실패 or 인식된 번호가 선택 학생 목록에 없음. 리뷰 단계에서 수동 매핑 가능 or 스킵
    - **🚫 중복 (`duplicate`)**: 같은 학생에 여러 파일이 매칭됨. 리뷰에서 정본 파일 선택 필요
  - **완료 처리**: 매칭 완료(`ok`) 학생만 상태 → `채점 확인`로 전환. 스킵·미매칭 학생은 `미채점` 유지
  - **완료 후 FAB 연계**: 스캔 채점도 기존 일괄 채점과 동일한 FAB 흐름 재사용
    - `handleScanCompleted(matchedStudentIds)` → `setLastBulkGradedIds([...matched])` + `setShowFAB(true)` + `setIsGradingFinished(true)`
    - FAB 클릭 → v3.15 정책대로 첫 성공 학생 SCR-03 상세 자동 오픈
  - **실패 처리**: 기존 일괄 채점과 동일 패턴 (완료 요약에서 실패 카운트, 재시도 별도 액션)
  - **UX 근거**: 스마트펜 미보유·오프라인 채점본이 있는 학교를 위한 대체 채점 경로. 정렬 부담 없이 스캔만 하면 OCR로 자동 분류
  - **JSX**: 신규 `src/ScanGradingModal.jsx` (독립 컴포넌트) + `GradingManagement.jsx`에 state·핸들러·버튼 추가
- **v3.15** | 2026-07-15 | **일괄 채점 완료 FAB — 「채점 완료」 문구 확정 + 클릭 시 첫 채점 학생 SCR-03 자동 진입**:
  - **문구 변경 (일괄 채점 완료 상태)**: `"채점이 완료되었습니다."` → `"✓ 채점 완료"` + hint `"탭하여 {학생명} 상세 열기"` (개별 채점 완료 문구는 유지)
  - **클릭 동작 변경 (일괄 채점 완료 상태)**:
    - **기존 (v3.12~v3.14)**: FAB 클릭 → 일괄 채점 워크플로우 모달(`final_bulk` 단계) 재오픈 → 완료 결과 요약 노출
    - **신규 (v3.15)**: FAB 클릭 → **`lastBulkGradedIds`(일괄 채점 대상 선택 순서 유지) 중 첫 성공 학생(status === '채점 확인')의 SCR-03 상세 모달 자동 오픈** → FAB 종료
    - 첫 성공 학생이 없으면 (전체 실패 등) 기존 `final_bulk` 모달 재오픈 fallback 유지
  - **UX 근거**: 완료 요약 확인 단계를 생략하고 곧바로 학생 검토 흐름으로 진입하도록 하여 채점 후 검토 사이클을 최소화. 요약이 필요하면 SCR-03에서 이전/다음 학생 네비게이션 통해 순차 확인 가능
  - **JSX 반영**: `GradingManagement.jsx`에 `lastBulkGradedIds` state 신규 추가 (일괄 채점 완료 시 `selectedIds` 스냅샷 저장), FAB 핸들러 완료 분기에 첫 성공 학생 pick 로직 추가. FAB 보더 색상도 완료 시 `#10B981`(초록)로 강조 전환
- **v3.14** | 2026-04-29 | 백그라운드 채점 진행 중 학생 선택 전체 차단(Option B) — FAB 활성 시 모든 학생 카드/행 체크박스 + 컨트롤 바·테이블 헤더 [전체 선택] + 일괄 작업 버튼 전체(일괄 채점/검토 완료/과정평가/결과발송/AI 재검토/결과발송 취소) 비활성. hover 안내 툴팁 통일. 의도: 진행 중 신규 일괄 작업 시작·선택 변경 실수 차단
- **v3.13** | 2026-04-29 | F5 새로고침 후 채점 완료 알림 정책 명시 — FAB이 복원되지 않는 F5 케이스에서도 완료 시점에 화면 하단 중앙 **완료 토스트** 노출(FAB 완료 상태와 동일 메시지 `"채점이 완료되었습니다."` + ✓ 아이콘 + 5초 페이드). 카드/행 진행 표시는 자동 해제. 일괄 채점·개별 채점 공통 정책. §3 보완 정책 도입부에 "진행 중 상세 진입 차단" 한 줄 명시 추가
- **v3.12** | 2026-04-29 | 백그라운드 채점 FAB 클릭 동작을 진행 중 / 완료 후 두 시나리오로 분리 명시 — (1) 진행 중 클릭 시 일괄: final_bulk 모달(진행률 그대로) / 개별: SCR-02 모달(진행 중 라벨 그대로, 학생 상태 = 미채점) 다시 오픈 (2) 완료 후 클릭 시 일괄: final_bulk 모달(완료 결과 요약) / 개별: SCR-03 모달 자동 오픈(학생 상태 = 채점 확인). SCR-02 §4도 동일 정책으로 동기화
- **v3.11** | 2026-04-29 | 학생 목록 카드/리스트 뷰 토글 명세 + 일괄 채점 진행 표시 추가 — (1) [▦ 카드][☰ 목록] 토글 정책(기본 카드, 상태 화면 단위 로컬, 접근성 `aria-pressed`) (2) 목록 보기 컬럼 정의(체크박스/이름/학년반번호/AI 채점/교사 채점/상태/상세보기) + 행 hover 강조 (3) **일괄 채점 진행 중 학생 표시** — 카드는 흰색 반투명 오버레이 + 점선 테두리 + 회전 스피너 + "AI 채점중...", 리스트는 옵션 A(AI 채점 셀에 칩)+옵션 B(행 디밍 + 좌측 4px 인디케이터) 합산 적용. 진행 중 체크박스/[상세보기] 비활성화. F5 새로고침 등으로 모달/FAB 상태가 사라져도 학생 목록 자체로 진행 상태 식별 가능 (서버 진행 복원 구현 전 보완 정책)
- **v3.10** | 2026-04-29 | 일괄 채점 모달 닫기 정책을 SCR-02와 동기화 — 진행 중 [×]/오버레이 클릭 시 즉시 닫지 않고 백그라운드 진행 확인 다이얼로그 노출, [닫기] 선택 시 FAB로 전환. `beforeunload` 차단 명문화
- **v3.9** | 2026-04-28 | 백그라운드 채점 FAB 정책을 SCR-01·SCR-02 공유로 명문화 — 일괄/개별 채점 메시지 분기, 클릭 시 분기(일괄: 일괄 모달 / 개별: SCR-03 자동 오픈), 동시 진행 금지(상호 [AI 등급평가 시작]/[일괄 채점] 비활성화) 규정. SCR-02와 동기화된 `ExitConfirmModal` 재사용 정책
- **v3.8** | 2026-04-28 | 채점 결과 내보내기 모달을 별도 PRD [POP-19](pop-19_채점결과내보내기.md)로 분리 — 학생 명단/포함 항목 선택/미리보기/ZIP 구조/PDF 리포트 포맷/상태별 변화는 POP-19 문서로 이동. SCR-01에는 진입점·진입 차단 조건만 남김
- **v3.7** | 2026-04-28 | 채점 결과 내보내기 포함 항목 선택 옵션 추가 — 모달에서 `문항 / 답안 / 교사 피드백(고정)` 중복 선택. 선택된 항목만 미리보기와 ZIP PDF에 노출. 교사 피드백은 항상 포함(체크 해제 불가)
- **v3.6** | 2026-04-17 | AI 과정평가 교과 제약 반영 — 일괄 과정평가 버튼은 과제 교과 = 수학일 때만 노출 (SCR-04 교과 제약과 동기화)
- **v3.5** | 2026-04-16 | 펜 목록 상태 4분류 재정의 — 정상 매핑/그룹 불일치/펜 충돌/학생 미매칭. 목록 기준을 크래들 연결 펜 전체로 전환, 학생 미매칭 펜은 회색 비활성 행으로 노출
- **v3.4** | 2026-04-16 | 펜 목록 노출 기준 및 상태 4분류(정상 매핑/그룹 불일치/펜 충돌/펜 없음) 명세 추가. 펜 없음 학생은 목록 미노출 정책 확정
- **v3.3** | 2026-04-16 | 학생 목록 정렬(기본순/이름순/학년반번호순) 명세 추가, 일괄 채점 크래들 이용 가이드 UI 명세 추가
- **v3.2** | 2026-04-14 | 채점 결과 내보내기 플로우 상세화 — 결과 발송 단계 학생 명단 모달 + ZIP 파일 구조 + 개별 PDF 리포트 포맷(5섹션) 정의. 과정평가 완료 학생은 AI 과정평가 섹션 자동 추가
- **v3.1** | 2026-04-14 | 리뷰 갭 필수 2건 반영 — 동시 일괄 채점 충돌 방지(과제 단위 서버 락), 결과 발송 타임스탬프 카드 노출 및 취소 유효기간 정책 명시
- **v3.0** | 2026-04-14 | PRD 템플릿 구조로 재생성 — 문서 이력 시간순 정렬, 일괄 버튼 섹션 재배치(채점/결과발송/과정평가 그룹 분리), 내용 보존
- **v2.3** | 2026-04-14 | 결과 발송 상태를 `결과 발송 전` / `결과 발송 완료` 2-단계로 분리. 일괄 버튼 3종(결과발송·AI 재검토·결과발송 취소) 추가, 상태 전환 규칙 보완
- **v2.2** | 2026-04-14 | 김순정 | 용어 정리 — "AI 채점" → "AI 등급평가" 통일, "필기 과정 평가" → "과정평가"
- **v2.1** | 2026-04-14 | 김순정 | 과정평가 기능 추가 — 일괄 과정평가 플로팅 버튼, 과정평가 완료 배지, 모든 탭 체크박스 노출
- **v2.0** | 2026-04-14 | 김순정 | PRD 템플릿 구조로 재생성 (내용 보존, 구조 변환)
- **v1.1** | 2026-04-14 | 김순정 | 리뷰 갭 전면 반영 — URL, 빈/로딩/에러 상태, 무효처리·OCR 라우팅, 일괄 채점 부분 실패 정책, 결과 발송 흐름
- **v1.0** | 2026-04-14 | 김순정 | GRD-01 분리, 채점관리 목록 화면 최초 작성

---

## 1. 화면 개요

- **화면 ID**: `SCR-01`
- **화면 이름**: 채점 관리 (Grading Management - List View)
- **URL 경로**: `/grading`
- **메뉴 경로**: `과제 및 채점관리 > 채점 관리`
- **대상 사용자**: 교사
- **비즈니스 목적**: 과제를 선택하고, 학생별 채점 상태(미채점 / 채점 확인 / 결과 발송 전 / 결과 발송 완료)를 한눈에 파악하며, 개별 상세보기 및 일괄 채점·결과발송·과정평가 워크플로우를 시작할 수 있는 채점 관리 허브 화면이다.
- **관련 파일**: `GradingManagement.jsx`

---

## 2. 주요 사용자 흐름 (Happy Path)

1. 좌측 메뉴 `과제 및 채점관리 > 채점 관리` 클릭 → `/grading` 진입
2. 좌측 과제 사이드바에서 과제 선택
3. 상단 과제 정보 배너에서 제출률·완료율 확인
4. 탭(전체 / 미채점 / 채점 확인 / 결과 발송)으로 필터링
5. 학생 카드에서 [상세보기] 클릭 → SCR-02 또는 SCR-03 모달 오픈
6. SCR-03 STEP 2에서 [검토 완료] 수행 시 해당 학생은 `결과 발송 전`로 전환되어 `결과 발송` 탭으로 자동 이동
7. (선택) 미채점 탭에서 학생 체크 → [일괄 채점] 실행 → 결과 요약 확인
8. (선택) 결과 발송 탭에서 `결과 발송 전` 학생 체크 → [결과발송] 실행 → `결과 발송 완료`로 전환
9. (선택) 결과 발송 탭에서 `결과 발송 완료` 학생 체크 → [결과발송 취소] 실행 → `결과 발송 전`로 되돌림
10. (선택) 임의 탭에서 AI 등급평가 완료 학생 체크 → [일괄 과정평가] 실행 → 결과 요약 확인

---

## 3. 화면 구성

### 좌측 과제 선택 사이드바

**전체 공통:**

- 검색 Input에 과제명/교과 입력하여 과제 목록 실시간 필터링
  - 성공: 일치 과제만 노출
  - 실패: "검색 결과가 없습니다" 센터 정렬 안내
- 과제 카드 클릭하여 해당 과제 선택
  - 성공: 파란 테두리 + shadow로 active 강조, 우측 배너·학생 그리드 로드
  - 조건: 동시 1개 과제만 선택 가능

### 과제 정보 배너

**전체 공통:**

- [과제 상세보기 >] 클릭하여 과제 상세로 이동
- [채점 결과 내보내기] 클릭하여 **채점 결과 내보내기 모달** 오픈 (→ [POP-19](pop-19_채점결과내보내기.md))
  - 조건: 클릭 시 과제에 `결과 발송 전` 또는 `결과 발송 완료` 상태 학생이 1명 이상 있어야 활성화. 0명이면 "결과 발송 단계의 학생이 없습니다." alert 후 차단
  - 모달 내에서 대상 학생 확인·포함 항목 선택(문항/답안/교사 피드백[고정]) → [📦 ZIP 내보내기] 실행으로 개별 학생 리포트를 ZIP 파일로 다운로드 (상세 동작은 POP-19)

### 탭 & 컨트롤 바

**control-bar 2단 레이아웃 (v3.18):**

control-bar는 다음과 같이 **세로 2단**으로 구성한다 (`flexDirection: column`):

```
┌─ control-bar ──────────────────────────────────────────────────┐
│ [미채점 (N)]  [채점 확인 (N)]  [결과 발송 (N)]                  │  ← 1단: 필터 탭 (좌측)
│ [그룹 ▾] [☑ 전체 선택] [일괄 채점 (N명)]          [▦] [☰] │  ← 2단: view-controls
└────────────────────────────────────────────────────────────────┘
```

**1단 — 필터 탭** (v3.18 「전체」 탭 폐기):
- **[미채점 (N)]** — 화면 진입 기본값
- **[채점 확인 (N)]**
- **[결과 발송 (N)]** — `결과 발송 전` + `결과 발송 완료` 포함

**2단 — view-controls** (좌측 정렬 + 뷰 토글은 우측 끝, `flexWrap: wrap` v3.20 적용):

좌측부터 자연 흐름 순서로 배치 — 그룹 정하기 → 학생 전체 선택 → 탭별 일괄 액션 실행:

1. **[그룹 선택 ▾]** 셀렉트박스
2. **[☑ 전체 선택]** 체크박스
3. **탭별 일괄 액션 버튼** — 모든 일괄 액션은 view-controls 안에 인라인 노출 (v3.20 통합. v3.20 이전 하단 floating `bulk-button-container` 폐기)
4. **[▦ 카드]** / **[☰ 목록]** 뷰 토글 — `marginLeft: auto`로 우측 끝 정렬

**탭별 일괄 액션 버튼 노출 매트릭스 (v3.20):**

| 탭 | 노출되는 버튼 | 노출 조건 | 색상 |
|:---|:---|:---|:---|
| 미채점 | [일괄 채점 (N명)] | `selectedIds.length > 0` | 기본 파랑 |
| 채점 확인 | [✓ 검토 완료 처리 (N명)] | `bulkReviewCompleteIds.length > 0` | `#E8590C` 주황 |
| 채점 확인 / 결과 발송 (수학 한정) | [✎ 일괄 과정평가 (N명)] | `activeTab !== '미채점' && bulkHwEligibleIds.length > 0 && currentTask.type === '수학'` | `#8B5CF6` 보라 |
| 결과 발송 | [📤 결과발송 (N명)] | `bulkPreSendIds.length > 0` | `#2A75F3` 파랑 |
| 결과 발송 (단일) | [↩ AI 재검토 (1명)] | `bulkPreSendIds.length === 1` | white + `#EF4444` 보더 |
| 결과 발송 완료 | [🚫 결과발송 취소 (N명)] | `bulkSentIds.length > 0` | `#EF4444` 빨강 |

- **공통 disabled 정책**: `isAnyBgActive` (백그라운드 채점 진행 중)이면 모든 일괄 버튼이 `opacity 0.5` + `cursor: not-allowed` + 안내 툴팁
- **하단 floating 폐기**: v3.20 이전 학생 목록 아래 `bulk-button-container` 영역은 통째로 제거되어 학생 카드/리스트가 바로 페이지 푸터(선택된 학생 없을 때)와 맞닿음

**전체 공통:**

- 탭(미채점/채점 확인/결과 발송) 클릭하여 학생 카드 필터링 (v3.18 — 「전체」 탭 폐기)
  - 성공: 해당 상태 학생만 노출, URL 쿼리 `?tab=ungraded|graded|sent` 갱신
  - 조건: `결과 발송` 탭은 `결과 발송 전` + `결과 발송 완료` 학생을 모두 포함
  - 기본값: 화면 진입 시 `미채점` 탭 활성 (교사가 가장 먼저 처리할 단계)
- 그룹 셀렉트박스 변경하여 그룹 필터링 (view-controls **첫 번째 항목**)
  - 성공: 해당 그룹 학생만 노출, URL 쿼리 `&group={id}` 갱신
  - 조건: 그룹 변경 시 [전체 선택] 체크박스 상태 초기화
  - **기본값 (v3.19 신설)**: 화면 진입 시 mount 1회 — `미채점` 상태인 학생 중 **첫 번째 학생의 그룹**을 자동 선택. 학생 데이터의 `grade` 필드(예: `'1학년 1반 1번'`)에서 「N번」 꼬리 제거하여 그룹명을 추출(`'1학년 1반'`). 사용자가 한 번이라도 그룹을 변경한 뒤에는 자동 로직 비활성. 미채점 학생이 0명이면 placeholder 「그룹 선택」 유지
  - 의도: 자연 흐름(그룹 정하기 → 전체 선택 → 일괄 채점)의 첫 단계를 자동 완료하여 교사가 [전체 선택]부터 바로 시작할 수 있게 함
- [전체 선택] 체크박스 클릭하여 현재 노출 학생 전체 선택 (view-controls **두 번째 항목**)
  - 조건: 모든 탭에서 상시 노출 (일괄 작업 대상 선택용)
- **탭별 일괄 액션 버튼 (v3.20 — 모든 일괄 액션이 view-controls로 통합)**
  - 위치: view-controls **세 번째 영역** ([☑ 전체 선택] 뒤, 뷰 토글 앞)
  - 노출 매트릭스: 위 「탭별 일괄 액션 버튼 노출 매트릭스」 표 참조 (탭별로 다른 버튼이 조건 만족 시 인라인 노출)
  - 클릭 동작:
    - [일괄 채점 (N명)] → 일괄 채점 워크플로우 모달 오픈
    - [✓ 검토 완료 처리 (N명)] → 검토 완료 처리 → 결과 발송 탭 이동
    - [✎ 일괄 과정평가 (N명)] → 과정평가 워크플로우
    - [📤 결과발송 (N명)] → 결과발송 확인 모달
    - [↩ AI 재검토 (1명)] → 채점 확인 단계로 환원
    - [🚫 결과발송 취소 (N명)] → 결과발송 취소
  - 비활성: 모든 일괄 버튼 공통 — `isAnyBgActive` 시 `opacity 0.5 + cursor not-allowed` + 안내 툴팁
  - 의도: 탭별로 위/아래를 번갈아 보던 동선 제거. 모든 탭에서 일관되게 「상단 view-controls = 액션 영역, 본문 = 학생 목록」 구조로 시선 흐름 단순화
  - 舊 하단 floating `bulk-button-container` 영역 v3.20 완전 폐기
- 뷰 컨트롤 토글 [▦ 카드][☰ 목록] 클릭하여 학생 목록 표시 방식 전환 `[UPDATE 2.1]` (view-controls **마지막 항목**)
  - 기본값: `카드 보기`
  - 성공: 적용된 탭/그룹/정렬·체크박스 선택 상태는 유지된 채 본문만 카드 그리드 ↔ 테이블로 즉시 전환
  - 조건: 화면 단위 로컬 상태 (페이지 이동 시 초기화)
  - 접근성: `role="button"` + `aria-pressed` 상태 반영
- 학생 목록 정렬 셀렉트 변경하여 학생 카드 정렬 순서 변경
  - 옵션: `기본순` (서버 반환 순서) / `이름순` (가나다 오름차순) / `학년반번호순` (학년→반→번호 오름차순)
  - 기본값: `기본순`
  - 성공: 즉시 학생 카드 재정렬, 선택 상태(체크박스) 유지
  - 조건: 모든 탭에서 동일하게 적용

### 학생 목록 (카드/리스트 공통)

**전체 공통:**

- [상세보기] 클릭하여 학생 상세 모달 오픈 (상태별 분기)
  - `미채점` · `답안 있음`: SCR-02 모달
  - `채점 확인`: SCR-03 모달 (STEP 2 — 교사 검토 진행중)
  - `결과 발송 전`: SCR-03 모달 (STEP 3 — 결과 발송 전, AI 재검토 가능)
  - `결과 발송 완료`: SCR-03 모달 (STEP 3 — 결과 발송 완료, 결과발송 취소 가능)
- 학생 행/카드 체크박스 클릭하여 일괄 작업 대상 선택
  - 조건: 모든 탭에서 체크박스 노출 (일괄 채점 / 결과발송 / 결과발송 취소 / 과정평가 대상 선택용)
  - 조건: 일괄 채점은 미채점·답안 있음 학생만, 일괄 과정평가는 AI 등급평가 완료(채점 확인·결과 발송 전·결과 발송 완료) 학생만 대상
  - 조건 — **백그라운드 채점 진행 중 차단** `[UPDATE 2.1]`: 일괄 또는 개별 백그라운드 채점이 진행 중이면 (FAB 활성 시) 다음 항목이 모두 비활성화된다. hover 시 안내 툴팁 "AI 채점이 진행 중입니다. 완료 후 다시 시도해 주세요."
    - 모든 학생 카드/행 체크박스 (이미 진행 중인 학생 + 그 외 학생 모두)
    - 컨트롤 바의 [전체 선택] 체크박스
    - 리스트 테이블 헤더의 [전체 선택] 체크박스
    - 일괄 작업 플로팅 버튼 전체 — [일괄 채점] / [✓ 검토 완료 처리] / [✎ 일괄 과정평가] / [📤 결과발송] / [↩ AI 재검토] / [🚫 결과발송 취소]
    - 의도: 진행 중에 새로운 일괄 작업을 시작하거나 선택을 변경하는 실수를 차단. 채점이 끝나기 전까지 학생 목록은 "조회 전용" 상태로 잠긴다 (단, 진행 중이 아닌 학생의 [상세보기]는 정상 활성)

#### 카드 보기 (기본)

- 카드 그리드 레이아웃 — 한 카드에 다음을 표시:
  - 좌상단 상태 배지(미채점/답안 있음/채점 확인/결과 발송 전/결과 발송 완료) + 과정평가 완료 배지(보라)
  - 학생 이름 + 학년/반/번호
  - "AI 채점", "교사 채점" 등급(또는 `-`)
  - 우상단 체크박스, 하단 [상세보기] 버튼

#### 목록(리스트) 보기 `[UPDATE 2.1]`

- 테이블 레이아웃 — 컬럼: `체크박스 / 학생 이름 / 학년·반·번호 / AI 채점 / 교사 채점 / 상태 / 상세보기`
  - 헤더 체크박스: 현재 노출 학생 전체 선택 토글
  - 상태 셀: 카드 보기와 동일한 상태 배지 컴포넌트 재사용 (미채점/답안 있음/채점 확인/결과 발송 전·완료)
  - 상세보기: 텍스트 링크 `상세보기 >` (hover 시 파란색 강조)
- 행 hover 시 옅은 배경(`#F8FAFC`) 강조
- 정렬·검색·체크박스 선택은 카드 보기와 완전 공유

#### 일괄 채점 진행 중 학생 표시 `[UPDATE 2.1]`

> F5 새로고침 등으로 진행 중 모달/FAB 상태가 사라져도 어느 학생이 채점 중인지 시각적으로 식별할 수 있도록, 학생 목록 자체에도 진행 표시를 노출한다. 진행 중 상세 진입은 차단되며, 진행 상태 확인은 카드/행의 시각적 표시로만 가능하다. (후속에 서버 진행 상태 복원이 구현되기 전까지의 보완 정책)

**카드 보기:**

- 진행 중 학생 카드에 **흰색 반투명 오버레이** 노출 (점선 파란 테두리)
- 오버레이 내부: 회전 스피너 + 굵은 파란색 텍스트 `"AI 채점중..."`
- 카드 본문은 35% 투명도 + 약한 grayscale 처리, 체크박스·[상세보기] 버튼 비활성화
- 채점 완료 시 자동 해제 — 오버레이/스피너/디밍 제거, 카드의 AI 채점 등급·교사 채점 등급·상태 배지가 갱신되어 정상 카드로 복귀

**목록(리스트) 보기:** (옵션 A + B 합산)

- 옵션 A — `AI 채점` 셀의 등급값을 진행 중 칩으로 교체
  - 칩 구성: 작은 회전 스피너(12px) + `"채점중..."` 텍스트
  - 색상: 배경 `#EBF2FF`, 텍스트 `#2A75F3`
- 옵션 B — 행 전체 시각 디밍 + 좌측 4px 인디케이터
  - 행 배경 `#F8FAFC`, 텍스트 셀 `#94A3B8`로 톤 다운
  - 행 좌측 첫 셀에 4px 너비 파란색(`#2A75F3`) 인디케이터 바
- 진행 중 행의 체크박스 비활성화, [상세보기 >] 링크는 회색 disabled 처리(클릭 불가, tooltip "AI 채점이 진행 중입니다.")
- 채점 완료 시 자동 해제 — 칩/인디케이터/디밍 제거되며 AI 채점 셀에 등급값 노출, 상태 배지가 `채점 확인`로 전환

##### F5 후 채점 완료 알림 `[UPDATE 2.1]`

> F5로 인해 백그라운드 채점 FAB은 복원되지 않지만, **채점 완료 시점의 알림은 In-session FAB 완료 동작과 동일한 메시지로 보장**한다. (일괄 채점 / 개별 채점 공통)

- **표시 방식**: 화면 하단 중앙에 **완료 토스트** 노출 (FAB과 동일 위치·동일 알약형 디자인)
  - 시각: 흰 배경 + 초록 ✓ 아이콘 + 굵은 텍스트
  - 메시지: `"채점이 완료되었습니다."` (FAB 완료 상태와 동일 문구)
  - 노출 시간: 약 5초간 자동 노출 후 페이드 아웃 (또는 사용자 클릭 시 즉시 닫힘)
- **트리거**: 화면이 폴링 또는 서버 알림으로 채점 완료를 감지한 시점 (학생 상태가 `채점 확인`로 전환되는 순간)
- **카드/행 동기화**: 토스트 노출과 동시에 해당 학생 카드/행은 진행 중 표시(오버레이/칩/인디케이터)가 자동 해제되고 정상 상태로 복귀
- **다중 완료 처리**: 일괄 채점에서 여러 학생이 동시 완료되어도 토스트는 1회만 노출 (메시지는 동일 — "채점이 완료되었습니다.")
- **In-session FAB 완료와의 차이**:
  - In-session: FAB이 ✓ 상태로 변하고 사용자가 클릭하기 전까지 유지 (클릭 → 모달 자동 오픈)
  - F5 후: FAB 자체가 없으므로 토스트로 대체, 별도 모달 자동 오픈 동작 없음. 사용자가 학생 카드/행을 직접 클릭해 SCR-03 진입
- **컴포넌트 재사용**: FAB 완료 상태(`grading-fab.finished`)의 시각 토큰을 그대로 재사용하여 일관성 유지

### 일괄 채점 플로팅 버튼

**교사 전용:**

- [일괄 채점 (N명)] 클릭하여 일괄 채점 워크플로우 시작
  - 성공: AiGLE Connector 설치 확인 후 워크플로우 모달 진행
  - 실패: AiGLE Connector 미설치 시 설치 안내 모달 + [다운로드] 버튼
  - 조건: 미채점 탭에서만 노출
  - 조건: 체크된 학생(미채점·답안 있음)이 1명 이상일 때만 노출
  - 조건: 그룹 미선택 시 "그룹이 선택되어야 일괄 채점을 시작할 수 있습니다." alert
  - 조건: 클릭 직후 버튼 비활성화 (중복 요청 방지)
  - **조건 — 동시 채점 충돌 방지(교사간)**: 동일 과제에 대해 다른 교사(담임·부담임 등)가 이미 일괄 채점 중이면 **선행 요청이 완료될 때까지 후행 요청 차단** + "다른 교사가 해당 과제를 일괄 채점 중입니다. 잠시 후 다시 시도해 주세요." alert. 과제 단위 서버 락(lock)으로 관리, 락 해제는 선행 워크플로우 완료/취소/타임아웃 시점
  - **조건 — 개별 채점과의 동시 진행 금지**: 동일 교사가 본인의 SCR-02 모달에서 개별 AI 등급평가를 백그라운드로 진행 중이면(아래 "백그라운드 채점 FAB" 활성), 본 버튼은 비활성화 + 안내 툴팁 "개별 채점이 백그라운드에서 진행 중입니다. 완료 후 다시 시도해 주세요."
- 워크플로우 진행 중 모달 닫기 시도(모달 [×] / 오버레이 클릭) 시 즉시 닫지 않고 **백그라운드 진행 확인 다이얼로그** 노출 (SCR-02 개별 채점과 동일 정책 — `ExitConfirmModal` 또는 동등 인라인 다이얼로그)
  - 메시지: `페이지를 벗어나도 AI 채점은 멈추지 않습니다.\n20분 이내로 채점이 완료 예정입니다.`
  - 액션: [계속 채점하기] → 다이얼로그만 닫힘, 모달 유지 / [닫기] → 모달 닫힘 + 채점 백그라운드 진행 + 화면 하단 중앙 백그라운드 채점 FAB 노출
- 워크플로우 진행 중 페이지 이탈(새로고침/탭 닫기/메뉴 이동) 시 `beforeunload` confirm 경고
- 워크플로우 `final_bulk` 단계 진입 시 **크래들 이용 가이드** UI 노출
  - 상단 가이드 배너: "💡 크래들 이용 가이드" (다크 배경, 접기/펼치기 토글)
  - 크래들 연결 상태: "● 크래들 연결됨" (초록) 실시간 표시
  - 요약 카운트 노출: 연결된 펜 N개 / 그룹 불일치 N개 / 학생 미매칭 N개 / 성공 N개
  - **학생 목록 정렬**: `기본순` / `이름순` / `학년반번호순` 토글 버튼 (칩 스타일, 선택 시 파란색 강조)
  - 펜 목록 테이블: 학생명 / 연결 상태 / 데이터 유무 / 배터리 / 펌웨어 버전 표시
  - 펌웨어 업데이트 필요 펜은 [🔄 업데이트] 버튼 노출
  - **펌웨어 업데이트 안내 (선택 · 권장 — 필수 아님)**: 가이드 상단에 안내 박스(파랑 톤) 노출. 안내 문구:
    - 제목: `📌 펌웨어 업데이트 (선택 · 권장)`
    - 본문: `펜 펌웨어를 최신 버전으로 업데이트하면 펜의 최신 기능을 이용하고 펜을 더 잘 활용할 수 있습니다. 각 펜의 [🔄 업데이트](개별) 또는 하단 [펌웨어 일괄 업데이트](일괄)로 갱신할 수 있습니다. 업데이트 중에는 펜을 제거하지 마세요. 완료 후 자동으로 최신 버전으로 표시됩니다.`
    - 강조: `ℹ️ 펌웨어 업데이트는 필수가 아니며, 업데이트하지 않아도 일괄 채점을 시작할 수 있습니다.`
    - **정책**: 펌웨어 미업데이트 펜이 있어도 **일괄 채점 시작을 차단하지 않는다** (기존 「필수·차단」 정책 폐기). [펌웨어 일괄 업데이트] 버튼에는 `(선택)` 보조 라벨 표기
  - **채점 완료 요약 블록** `[v4.5 — 舊 「채점 완료: 모든 펜의 채점이 끝났습니다.」 한 줄 배너]`

    SCR-05 스캔 채점 완료 단계(§8.2)와 **같은 지표**를 쓴다. 경로가 달라도 교사가 확인해야 하는 값은 같다.

    | 줄 | 내용 |
    |:--|:--|
    | 제목 | `일괄 채점이 완료되었습니다.` |
    | 요약 | `채점 문항 N건 · 학생 M명` |
    | 실패 고지 (있을 때만) | `⚠ N명은 채점되지 않아 미채점 탭에 그대로 남습니다. 펜 데이터를 확인한 뒤 다시 실행해 주세요.` |
    | 마무리 | `전 문항이 채점된 N명이 「채점 확인」 단계로 이동했습니다.` |

    - **집계 기준**: 완료 시점에 `selectedIds`가 비워지므로 스냅샷 `lastBulkGradedIds`로 계산한다. **학생 수** = 그중 `채점 확인`으로 전환된 수, **문항 수** = 학생 수 × 과제 문항 수
    - **문구가 과거형인 이유**: 펜 경로는 상태 전환이 **채점 완료 시점에 이미 끝나** 있고 버튼도 [확인]이 아니라 [닫기]다. 스캔 경로의 「[확인]을 누르면 … 이동합니다」를 그대로 쓰면 사실과 다르다

#### 펜 목록 노출 기준 및 상태 분류

> 목록 기준: **크래들에 연결된 펜 전체**. 펜 수만큼 행이 노출된다. 선택 학생 중 매칭되는 펜이 없는 학생은 목록에 노출하지 않는다.

| 상태                      | 조건                                                              | 행 표시                                                                                                                     |
| ------------------------- | ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| **정상 매핑**             | 펜의 번호표 그룹 = 선택 그룹이고, 선택 학생과 1:1 매칭됨          | 기본 행 스타일                                                                                                              |
| **그룹 불일치**           | 펜의 번호표 그룹 ≠ 선택 그룹                                      | 주황 경고 행 — 상태 메시지: `그룹 불일치 [감지: {감지반} → 선택: {선택반}]` (예: `그룹 불일치 [감지: 1-2반 → 선택: 1-1반]`) |
| **펜 충돌 (중복 데이터)** | 동일 펜이 다른 출석부에도 배정되어 있음                           | 경고 행 — 상태 메시지: `중복 데이터`                                                                                        |
| **학생 미매칭**           | 크래들에 연결된 펜이지만, 선택된 학생 목록에 대응하는 학생이 없음 | 회색 비활성 행 — 상태 메시지: `학생 미매칭` (채점 대상에서 제외)                                                            |

- **그룹 불일치**: 각 펜 행의 상태 컬럼에 감지반·선택반을 인라인 표시. `bulkStatus === 'ready'` 상태에서만 노출
- **펜 충돌**: 행에 "중복 데이터" 상태 뱃지 노출. 채점 실행은 차단하지 않되 교사에게 인지시킴
- **학생 미매칭**: 펜 정보(배터리·펌웨어 등)는 정상 표시하되 학생명 컬럼은 `-`로 표시. 채점 대상에서 자동 제외
- 완료 시 결과 요약 모달 표시 (성공 N명 / 실패 M명)
- [실패자 재시도] 클릭하여 실패 학생만 재실행
  - 성공: 성공한 학생은 `채점 확인` 탭으로 즉시 반영

### 스캔 일괄 채점 `[정본: SCR-05]`

> 📄 **스캔 일괄 채점의 전체 명세는 [SCR-05] 스캔 일괄 채점 문서가 정본이다.**
> 워크플로우 5-Step · OCR 매핑 정책 · 신뢰도 트리아지 · 슬롯 매트릭스 · 덮어쓰기 3단 게이트 · 부분 제출 처리 · 데이터 정책이 모두 그쪽에 있다.
> 본 절은 **SCR-01 화면에서 보이는 부분과 상호 영향만** 요약한다. 사양 변경 시 SCR-05를 먼저 갱신하고 본 절과 이력을 동기화한다.

**SCR-01에서 보이는 부분**

- **진입**: 미채점 탭 컨트롤 바 [일괄 채점 (N명)] 우측의 **[📷 스캔 채점 (N명)]** (하늘색 `#0EA5E9`)
  - 활성 조건: 그룹 선택 + 학생 1명 이상 선택 / 비활성: `isAnyBgActive`
- **대상 학생**: 체크된 학생을 모달에 넘기되, **모달은 `selectedIds`를 변경하지 않는다.** 대상 범위 축소는 모달 내부에서만 일어난다
- **`답안 있음` 학생**: 스캔 채점에서 **덮어쓰기 대상**이 되므로 모달 1단 게이트에서 기본 제외된다 (§학생 목록 상태 배지 참조)
- **완료 후 상태 전환**: **전 문항이 채점된 학생만** `채점 확인` 탭으로 이동한다. 일부 문항만 채점된 학생은 **`미채점` 탭에 그대로 남는다** — 부분 채점용 새 상태값은 만들지 않는다
- **FAB 연계**: 기존 일괄 채점과 동일. `handleScanCompleted(studentIds)` → FAB 노출 → 클릭 시 첫 성공 학생 SCR-03 자동 오픈 (v3.15 정책)

**왜 스캔에만 별도 정책이 필요한가**

펜 일괄 채점은 크래들에 해당 학생 펜이 없으면 「학생 미매칭」으로 자동 제외되어 자연 방어가 된다(§펜 목록 노출 기준). 반면 스캔은 **교사가 파일을 직접 업로드**하므로 그 방어막이 없다. 덮어쓰기·결손 판정 같은 보호 장치가 스캔 경로에만 있는 이유다.

**연계 문서**

| 문서 | 관계 |
|:--|:--|
| [SCR-05] 스캔 일괄 채점 | **정본.** 전체 명세 |
| [TSK-02] 과제 등록 | 「답안지 출력 장수 설정」 = 문항별 기준 장수 → 결손 판정 기준 |
| [TSK-05] 문답지 관리 | 답안지 서식 = OCR 판독 대상 (과제코드 인쇄값 + 문항 기재란) |

---

### 백그라운드 채점 FAB `[UPDATE 2.1]`

**교사 전용:** SCR-01 화면 하단 중앙에 알약형 플로팅 버튼으로 노출되며, 일괄 채점·개별 채점(SCR-02) 양쪽이 공유한다.

- **노출 조건**:
  - 일괄 채점 워크플로우 진행 중 사용자가 [닫기] 후 [백그라운드 진행]을 선택한 경우
  - SCR-02 모달에서 [AI 등급평가 시작] 후 [×] 또는 ESC로 모달을 닫으면서 [백그라운드 진행]을 선택한 경우
- **메시지 분기**:
  - 일괄 채점 진행 중: `"AI 가 채점하고 있어요."` + 보조 문구 `"잠시만 기다려 주세요."` + 회전 스피너 (파란 보더 `#2A75F3`)
  - 개별 채점 진행 중: `"AI가 {학생명} 채점하고 있어요."` + 보조 문구 `"잠시만 기다려 주세요."` + 회전 스피너 (파란 보더)
  - 일괄 채점 완료 `[UPDATED v3.15]`: **`"✓ 채점 완료"`** (초록 톤 `#047857`) + 보조 문구 `"탭하여 {첫 채점 학생명} 상세 열기"` + ✓ 아이콘 (스피너 정지) + **초록 보더 `#10B981`** (완료 강조)
    - 첫 채점 학생 정의: `lastBulkGradedIds` 배열(일괄 채점 시 선택 순서 유지) 중 `status === '채점 확인'`인 첫 학생 (성공 케이스만)
    - 첫 성공 학생이 없으면 보조 문구는 `"탭하여 결과 확인"` 로 fallback
  - 개별 채점 완료: `"채점이 완료되었습니다."` (기존 유지) + 보조 문구 `"탭하여 상세 확인"` + ✓ 아이콘
- **클릭 동작 — 진행 중 (채점 미완료) 클릭 시**:
  - 일괄 채점: 일괄 채점 워크플로우 모달(`final_bulk` 단계) 다시 오픈, 진행 중 펜 목록·진행률 그대로 노출 → FAB 사라짐
  - 개별 채점: 해당 학생의 **SCR-02 모달** 다시 오픈, [AI 등급평가 진행 중...] 라벨·다른 액션 비활성 상태 그대로 복원 (학생 상태 = `미채점`) → FAB 사라짐
- **클릭 동작 — 화면 밖에서 채점 완료된 뒤 클릭 시**:
  - **일괄 채점 `[UPDATED v3.15]`**: **첫 성공 학생의 SCR-03 상세 모달 자동 오픈** (기존 `final_bulk` 요약 재오픈 폐기)
    - 대상: `lastBulkGradedIds` 중 `status === '채점 확인'`인 첫 학생 (선택 순서 유지)
    - fallback: 첫 성공 학생이 없으면 기존 `final_bulk` 모달 재오픈 (완료 결과 요약 노출)
    - FAB·`isGradingFinished`·`lastBulkGradedIds` 모두 클리어 → FAB 사라짐
    - **UX 근거**: 완료 요약 확인 단계 생략, 곧바로 학생 검토 흐름 진입으로 사이클 최소화. 요약 필요 시 SCR-03 내 이전/다음 학생 네비게이션으로 순차 확인
  - 개별 채점: 학생 상태가 `채점 확인`로 전환되어 있으므로 **SCR-03 모달** 자동 오픈 (STEP 2 — 교사 검토 진행) → FAB 사라짐
- **공통**: FAB은 클릭 시점의 학생/일괄 상태에 따라 어느 모달이 열릴지 자동 결정 (구현은 `setIsModalOpen(true)` + 학생 상태 매칭 분기). 완료 여부와 무관하게 한 번 클릭하면 FAB은 닫힘
- **동시 진행 금지**:
  - FAB 활성 시 모든 [AI 등급평가 시작]·[일괄 채점] 버튼은 비활성화
  - 안내 툴팁: "다른 학생의 채점이 진행 중입니다. 완료 후 다시 시도해 주세요." / "개별 채점이 백그라운드에서 진행 중입니다. 완료 후 다시 시도해 주세요."
- **페이지 이탈 차단**:
  - 채점 진행 중에는 페이지 새로고침/탭 닫기/메뉴 이동 시 `beforeunload` confirm 노출

### 채점 확인 단계 일괄 버튼

**교사 전용:**

- [✓ 검토 완료 처리 (N명)] 클릭하여 선택 학생 일괄 검토 완료
  - 성공: 선택 학생의 상태가 `채점 확인` → `결과 발송 전`로 전환, "결과 발송" 탭으로 이동
  - 실패: "검토 완료 처리에 실패했습니다. 다시 시도해 주세요." 토스트
  - 조건: 체크된 학생 중 `채점 확인` 상태가 1명 이상일 때만 노출
  - 조건: 대상은 `채점 확인` 학생만 (혼합 체크 시 해당 상태 학생만 처리)

> **[↺ 미채점 처리]는 일괄 액션 미제공** — 채점 결과·교사 피드백·과정평가 결과 폐기 등 영향 범위가 큰 액션이므로 **학생 단위 신중 처리 정책**을 적용한다. 일괄 환원은 제공하지 않으며, [↺ 미채점 처리]는 **SCR-03 상세 모달 푸터 좌측**에서 학생 단위로만 실행한다. AI 채점 카운터 공유(최대 2회 정책 보존)·확인 메시지·환원 후 스캔 업로드 가능 정책은 **SCR-03 §3 「미채점 처리 버튼」** 명세를 따른다.

### 결과 발송 단계 일괄 버튼

**교사 전용:**

- [📤 결과발송 (N명)] 클릭하여 선택 학생 일괄 결과 발송
  - 성공: 선택 학생의 상태가 `결과 발송 전` → `결과 발송 완료`로 전환, "{N}명에게 결과가 발송되었습니다." 토스트
  - 실패: "결과 발송에 실패했습니다. 다시 시도해 주세요." 토스트
  - 조건: 체크된 학생 중 `결과 발송 전` 상태가 1명 이상일 때만 노출
  - 조건: 대상은 `결과 발송 전` 학생만 (혼합 체크 시 `결과 발송 전` 학생만 처리)

- [↩ AI 재검토 (1명)] 클릭하여 단일 학생을 `채점 확인` 단계로 되돌림
  - 성공: 해당 학생 상태가 `결과 발송 전` → `채점 확인`로 전환
  - 조건: 체크된 학생 중 `결과 발송 전` 상태가 **정확히 1명**일 때만 노출
  - 조건: `결과 발송 완료` 상태에서는 이 버튼이 노출되지 않으며, 먼저 [결과발송 취소]를 거쳐야 함

- [🚫 결과발송 취소 (N명)] 클릭하여 선택 학생 일괄 결과 발송 취소
  - 성공: 선택 학생의 상태가 `결과 발송 완료` → `결과 발송 전`로 전환
  - 조건: 체크된 학생 중 `결과 발송 완료` 상태가 1명 이상일 때만 노출

### 채점 결과 내보내기 모달

> 본 모달은 별도 PRD **POP-19 채점 결과 내보내기**로 분리되었습니다. 학생 명단·포함 항목 선택(문항/답안/교사 피드백[고정])·미리보기·ZIP 출력·PDF 리포트 포맷·상태별 변화는 [POP-19](pop-19_채점결과내보내기.md) 문서를 참조.

- 진입점: SCR-01 과제 정보 배너의 [채점 결과 내보내기] 버튼 클릭 시 POP-19 모달 오픈
- 진입 차단: 해당 과제의 `결과 발송 전` + `결과 발송 완료` 학생이 0명이면 "결과 발송 단계의 학생이 없습니다." alert 후 차단

### 일괄 과정평가 플로팅 버튼

**교사 전용:**

- [일괄 과정평가 (N명)] 클릭하여 일괄 과정평가 시작
  - 성공: 체크된 학생 전원에 대해 AI 과정평가 실행
  - 실패: 네트워크 에러 등 실패 시 토스트 + 재시도 가능
  - 조건: 모든 탭에서 노출 (전체/미채점/채점 확인/결과 발송)
  - 조건: **과제 교과 = 수학**일 때만 버튼 노출 (비수학 교과에서는 버튼 자체 숨김). SCR-04의 교과 제약과 동기화
  - 조건: 체크된 학생 중 AI 등급평가 완료(`채점 확인` 또는 `결과 발송 전` 또는 `결과 발송 완료` 상태)인 학생이 1명 이상일 때만 노출
  - 조건: 체크된 학생 중 AI 등급평가 미완료 학생은 평가 대상에서 자동 제외되며 경고 문구 "AI 등급평가가 완료된 학생만 과정평가됩니다" alert
  - 조건: 클릭 직후 버튼 비활성화 (중복 요청 방지)
- 진행 중 페이지 이탈 시 `beforeunload` confirm 경고
- 진행률 표시: 모달 내 프로그레스 바 (`M/N 완료`)
- 완료 시 결과 요약 모달 표시 (성공 N명 / 실패 M명)
- [실패자 재시도] 클릭하여 실패 학생만 재실행
  - 성공: 성공한 학생의 카드에 `과정평가 완료` 배지 즉시 반영

---

## 4. 상태별 변화

### SCR-01:과제 미선택

- **상태 설명**: 화면 진입 직후, 아직 어떤 과제도 선택하지 않은 초기 상태
- **전환 트리거**: `과제 및 채점관리 > 채점 관리` 메뉴 클릭 → SCR-01:과제 선택

#### 안내 메시지

- **메시지**: "좌측에서 과제를 선택하세요"
- **액션**: 과제 정보 배너 비활성, 학생 그리드 영역 플레이스홀더 노출

---

### SCR-01:과제 선택

- **상태 설명**: 과제가 선택되어 배너·학생 그리드가 정상 로드된 상태
- **전환 트리거**: 좌측 과제 카드 클릭 → 현재 상태 진입, URL `/grading?taskId={id}&tab=all`

#### 학생 목록 (카드/리스트 공통)

**전체 공통:**

- 채점 상태 배지 노출 (단일 선택, 카드는 좌상단 / 리스트는 `상태` 컬럼)
  - `미채점` (빨강): 과제 배포 후 **학생 답안이 없는** 상태. AI 등급평가 전
  - `답안 있음` (파랑) `[v3.24 — 舊 「OCR 제출」]`: **채점 전이지만 학생 답안이 이미 존재**하는 상태 (파일 업로드·키보드 입력 등 제출 수단 무관). AI 등급평가 전
    - `미채점`과 동일하게 `status === '미채점'`의 하위 구분(`submitType`)이며 **미채점 탭에 함께 노출**된다. 별도 탭·필터를 만들지 않는다
    - 스캔 채점 시 이 상태의 학생은 **덮어쓰기 대상**이 되므로 별도 게이트를 거친다 ([SCR-05] 스캔 일괄 채점 §6)
  - `채점 확인` (파랑): AI 등급평가 완료, 교사 검토 대기
  - `결과 발송 전` (주황): 교사 검토 완료 상태. 발송 버튼으로 발송 가능, AI 재검토로 `채점 확인`으로 되돌릴 수 있음
  - `결과 발송 완료` (초록): 학생에게 결과 발송 완료. `결과발송 취소`로만 `결과 발송 전`으로 되돌릴 수 있음
- 필기 평가 배지 (채점 상태 배지와 병행 노출)
  - `과정평가 완료` (보라): AI 과정평가가 완료된 학생 (채점 상태와 무관하게 누적 표시)
- **결과 발송 타임스탬프 표기**: `결과 발송 완료` 상태 학생에 발송 시각을 함께 노출 (예: `발송: 04.15 14:32`). 운영 문의 시 참조 용도
- **결과 발송 취소 유효기간**: 별도 제한 없음. `결과 발송 완료` 상태에서는 언제든 [결과발송 취소]를 실행해 `결과 발송 전`로 되돌릴 수 있음 `[정책 확인 필요]` (운영상 유연성 확보 기본값, 정책팀 확정 필요)

---

### SCR-01:로딩

- **상태 설명**: 과제 목록 또는 학생 카드 데이터 로드 중
- **전환 트리거**: 화면 진입 / 과제 선택 / 탭 전환 / 새로고침

#### 안내 메시지

- **메시지**: (없음 - 스켈레톤 UI)
- **액션**: 과제 사이드바에 스켈레톤 카드 3개, 학생 그리드에 스켈레톤 카드 8개 노출

---

### SCR-01:빈 상태 - 과제 없음

- **상태 설명**: 교사에게 배포된 과제가 한 건도 없는 상태
- **전환 트리거**: 과제 목록 API 결과 0건

#### 안내 메시지

- **메시지**: "배포된 과제가 없습니다"
- **액션**: 보조 문구 "과제 관리에서 과제를 배포한 후 이용하세요" + [과제 관리로 이동 >] 링크 → TKS-01

---

### SCR-01:빈 상태 - 탭 필터 0건

- **상태 설명**: 과제는 선택되었으나 현재 탭의 필터 결과가 0건
- **전환 트리거**: 탭 전환 또는 그룹 변경 후 필터 결과 0건

#### 안내 메시지

- **메시지** (탭별):
  - 미채점 탭: "미채점 학생이 없습니다"
  - 채점 확인 탭: "확인 대기 중인 학생이 없습니다"
  - 결과 발송 탭: "결과 발송 단계 학생이 없습니다"
  - 전체 탭: "이 과제에 할당된 학생이 없습니다"
- **액션**: 센터 정렬 안내 문구만 노출

---

### SCR-01:에러

- **상태 설명**: 과제 목록 또는 학생 카드 API 호출 실패
- **전환 트리거**: 네트워크 에러, 서버 에러 등

#### 안내 메시지

- **메시지**: "과제를 불러오지 못했습니다" / "학생 정보를 불러오지 못했습니다"
- **액션**: 토스트 알림 + 해당 영역 중앙에 [다시 시도] 버튼

---

### SCR-01:일괄 채점 진행

- **상태 설명**: NeoStudio2Lite 연동 후 일괄 채점 워크플로우 실행 중
- **전환 트리거**: [일괄 채점] 클릭 + NeoStudio2Lite 설치 확인 완료

#### 안내 메시지

- **메시지**: 워크플로우 모달 내 "M/N 완료" 프로그레스 바
- **액션**: 페이지 이탈 차단 confirm, 모달 외부 클릭 차단

---

### SCR-01:일괄 채점 결과

- **상태 설명**: 일괄 채점 완료 후 결과 요약 표시
- **전환 트리거**: 일괄 채점 워크플로우 완료

#### 안내 메시지

- **메시지**: "성공 N명 / 실패 M명" + 실패 학생 이름·사유 목록
- **액션**: [실패자 재시도] / [닫기] 버튼. 닫기 시 성공 학생은 `채점 확인` 탭으로 즉시 반영

---

### SCR-01:일괄 과정평가 진행

- **상태 설명**: 일괄 과정평가 실행 중
- **전환 트리거**: [일괄 과정평가] 클릭

#### 안내 메시지

- **메시지**: 진행 모달 내 "M/N 완료" 프로그레스 바
- **액션**: 페이지 이탈 차단 confirm

---

### SCR-01:채점 결과 내보내기 (POP-19 진입)

- **상태 설명**: [채점 결과 내보내기] 클릭 시 [POP-19](pop-19_채점결과내보내기.md) 모달이 오픈되는 상태. 모달 내 상세 동작·상태별 변화는 POP-19 문서를 참조
- **전환 트리거**:
  - [채점 결과 내보내기] 클릭 → POP-19 모달 오픈
  - 모달 내 [📦 ZIP 내보내기] 성공 → 모달 닫힘, `SCR-01:과제 선택`으로 복귀
  - 결과 발송 단계 학생 0건 → 모달 진입 차단 (아래 안내 메시지)

#### 안내 메시지 (리스트 0건)

- **메시지**: "결과 발송 단계의 학생이 없습니다."
- **액션**: [채점 결과 내보내기] 버튼 클릭 시 alert 후 모달 진입 차단

---

### SCR-01:일괄 과정평가 결과

- **상태 설명**: 일괄 과정평가 완료 후 결과 요약 표시
- **전환 트리거**: 일괄 과정평가 완료

#### 안내 메시지

- **메시지**: "성공 N명 / 실패 M명" + 실패 학생 이름·사유 목록
- **액션**: [실패자 재시도] / [닫기] 버튼. 닫기 시 성공 학생의 카드에 `과정평가 완료` 배지 즉시 반영

---

## 5. 관련 화면

- **SCR-02**: 미채점 상세 - 미채점·답안 있음 학생 [상세보기] 클릭 시 오픈. SCR-02 [무효처리] 수행 시 해당 학생은 본 화면의 `결과 발송` 탭으로 이동
- **SCR-03**: AI 등급평가 상세 - `채점 확인` / `결과 발송 전` / `결과 발송 완료` 학생 [상세보기] 클릭 시 오픈. 진입 시 `AI 등급평가` 탭 기본 활성. STEP별 푸터 버튼 분기(검토 완료 / 결과발송 / 결과발송 취소 / AI 재검토)로 학생 상태가 본 화면에 실시간 반영됨. 이전/다음 학생 순서는 본 화면의 현재 탭+그룹 필터 기준
- **SCR-04**: AI 과정평가 상세 - SCR-03 모달의 `AI 과정평가` 탭. 탭 전환으로 진입. 본 화면의 일괄 과정평가 기능으로 사전 처리된 학생은 SCR-04 진입 시 결과만 바로 표시
- **POP-19**: 채점 결과 내보내기 - 과제 정보 배너의 [채점 결과 내보내기] 클릭 시 오픈되는 모달. 학생 명단·포함 항목 선택(문항/답안/교사 피드백[고정])·미리보기·ZIP 출력·PDF 리포트 포맷을 정의
- **TKS-01**: 과제 관리 목록 - 과제 없음 빈 상태에서 유도 링크 대상
- **AI-04**: 스마트펜 연동 - 일괄 채점 워크플로우의 NeoStudio2Lite 설치 확인 및 연동
