# [AI-01] Prompt Studio 화면 개발 명세서

## 0. 문서 이력 (Revision History)

| 버전 | 날짜       | 수정자      | 내용                                                    |
| :--- | :--------- | :---------- | :------------------------------------------------------ |
| v1.3 | 2026-03-12 | Antigravity | 2번 영역과 3번 상태 ID 간 매칭 강화 (Traceability 개선) |
| v1.2 | 2026-03-12 | Antigravity | 상태 ID 명칭 최적화 (AI-01:ANALYSIS_RESULT)             |
| v1.0 | 2026-03-12 | 김순정      | 최초 작성                                               |

---

## 1. 화면 개요 (Overview)

- **화면 ID**: `AI-01 `
- **화면명**: Prompt Studio
- **URL 경로**: `/ai/prompt-studio`
- **대상 사용자**: AiGLE 개발자
- **접근 권한**: 'neolab.net' 계정인 id 교사 사용자에게만 노출
- **Figma Node ID**: `미정`
- **비즈니스 목적**: 등록된 문제를 선택하고 학생 답안지를 업로드한 뒤, 시스템 프롬프트를 AI 모델에 전달하여 원하는 채점 결과를 테스트하고 관리할 수 있게 한다.

---

## 2. 화면 요소 및 기능 (Elements & Functions)

_디자인 속성(색상, 간격)은 Figma에서 확인합니다. 여기서는 데이터와 기능 위주로 기술합니다._

### 2.1 등록 문항 선택 영역 (Step 1)

- **주요 요소:**
  - `Select Box`: 채점할 테스트 문항 선택 / 초기값: 없음
  - `선택 문항 정보 패널`: 문제, 모범답안, 채점기준 데이터 표시 영역
- **기능 정책:**
  - 문항 선택 시 해당 데이터를 백엔드에서 호출
  - 문항 변경 시 기존의 모든 분석 결과는 초기화됨

### 2.2 학생 답안 이미지 업로드 영역 (Step 2)

- **주요 요소:**
  - `Upload Zone`: 드래그 앤 드롭 또는 파일 선택 (이미지, PDF)
  - `미리보기 이미지`: 업로드된 파일의 프리뷰 노출
- **기능 정책:**`
  - 파일 삭제 시 `AI-01:DEFAULT` 상태의 초기 UI로 복구

### 2.3 시스템 프롬프트 설정 영역 (Step 3)

- **주요 요소:**
  - `OCR/채점 프롬프트 TextArea`: 편집 가능한 텍스트 영역
  - `저장/복구 버튼`: 로컬 스토리지 기반 기본값 관리
- **기능 정책:**
  - 편집 중인 내용은 AI 실행 시 파라미터로 전달됨

### 2.4 AI 모델 및 파라미터 설정 (우측 사이드바)

- **주요 요소:**
  - `모델/파라미터 컨트롤러`: 모델 선택 단추 및 슬라이더 UI
- **기능 정책:**
  - 선택 모델에 따라 하위 파라미터 항목 유동적 변경

### 2.5 AI 실행 버튼 영역

- **주요 요소:**
  - `AI 실행 버튼`: 화면 최하단 고정 버튼
- **기능 정책:**
  - **활성화**: 2.1 및 2.2 영역 입력 완료 시
  - **클릭 시**: 상태를 `AI-01:LOADING`으로 전환 및 API 호출

### 2.6 채점 결과 카드 영역

- **주요 요소:**
  - `분석 데이터 패널`: OCR 결과, AI 피드백, 비용/시간 정보 포함
  - `아카이브 저장 버튼`: 결과를 AI-02 화면으로 전송
- **기능 정책:**
  - **노출 시점**: `AI-01:ANALYSIS_RESULT` 상태 시에만 화면 하단에 동적으로 생성/노출
  - 저장 완료 후 버튼은 `Disabled` 처리되어 중복 저장 방지

---

## 3. 상태별 대응 (State & Interaction)

_각 상태는 `화면ID:상태ID` 형식으로 Figma 레이어와 매칭됩니다._

### 3.1 AI-01:AI실행전

- **상태 ID**: `AI실행전`
- **발생 조건**: 화면 최초 진입 시 또는 데이터 초기화 시
- **UI/UX**:
  - **섹션 2.1 ~ 2.5**가 기본 노출됨
  - **섹션 2.6(결과 카드)**은 숨김 처리됨

### 3.2 AI-01:AI실행전

- **상태 ID**: `AI실행전`
- **발생 조건**: 2.5 버튼 클릭 후 API 응답 대기 시
- **UI/UX**:
  - **섹션 2.5(버튼)**: 텍스트 변경(채점 중...) 및 클릭 차단
  - **섹션 2.6 영역**: 스켈레톤 UI 또는 로딩 애니메이션 노출 시작

### 3.3 AI-01:실행후

- **상태 ID**: `AI실행후`
- **발생 조건**: AI 분석 성공 및 데이터 수신 완료 시
- **UI/UX**:
  - **섹션 2.6(채점 결과 카드)**이 활성화되어 실제 데이터를 바인딩함
  - 기존 로딩 UI가 결과 카드로 교체됨
  - 결과 위치로 자동 스크롤 또는 강조 애니메이션 적용

### 3.4 AI-01:AI실행전

- **상태 ID**: `AI실행전`
- **발생 조건**: API 에러 또는 타임아웃 발생 시
- **UI/UX**:
  - 에러 알림 제공 후 **AI-01:DEFAULT** 상태의 버튼으로 복구

---

## 4. 비즈니스 로직 (Business Rules)

- **입력 유효성 판단**: 좌측의 `selectedAssignId`(문항)와 `studentImage`(답안 이미지)가 모두 존재할 때만 AI 실행 버튼 활성화
- **모의 데이터 채점 연산**: 현재 버전에서는 MockData 기반의 Promise(setTimeout 2초 딜레이) 처리하여 결과값 노출 (추후 실제 AI API 연동 예정)
- **비용 환율 환산**: `$ 0.00085`와 같이 반환된 USD 기준 비용에 `exchangeRate`(기본 1350원 기준, 새로고침 시 난수 반영)를 곱하여 원화(KRW)로 노출
- **프롬프트 기본값 관리**: 사용자가 저장한 프롬프트 기본값은 LocalStorage에 보관하여 다음 접속 시 자동 반영
- **문항 변경 시 초기화**: 문항을 변경하면 기존 결과 데이터 및 로드 상태가 모두 초기화됨

---

## 5. 예외 및 엣지 케이스 (Edge Cases)

- **데이터 전송 오류**: AI 분석 중 서버 통신이 끊길 경우 "서버와 연결이 원활하지 않습니다. 다시 시도해 주세요." 토스트 노출
- **분석 결과 형식 불일치**: AI가 규격화된 형식을 반환하지 않을 경우, 'AI 채점 결과 (Raw)' 영역에 수신된 텍스트 전체 노출
- **이미지 업로드 실패**: 지원하지 않는 파일 형식이거나 용량 초과 시 파일 선택을 취소하고 경고 문구 출력
- **프롬프트 저장 이력 없음**: `[초기값으로 복구]` 클릭 시 저장 이력이 없으면 시스템 최초 초기값을 로드
- **등록 문항 없음**: 등록된 과제가 없을 경우 Select Box에 "등록된 문항이 없습니다" 안내 노출

---

## 6. 관련 화면 (Related Screens)

- **`AI-02`**: Prompt 아카이브 - 저장된 테스트 결과를 목록으로 모아보고 상세 분석 리포트를 생성하는 화면
